"use strict";var __createBinding=this&&this.__createBinding||(Object.create?function(n,e,t,o){void 0===o&&(o=t);var a=Object.getOwnPropertyDescriptor(e,t);a&&!("get"in a?!e.__esModule:a.writable||a.configurable)||(a={enumerable:!0,get:function(){return e[t]}}),Object.defineProperty(n,o,a)}:function(n,e,t,o){void 0===o&&(o=t),n[o]=e[t]}),__setModuleDefault=this&&this.__setModuleDefault||(Object.create?function(n,e){Object.defineProperty(n,"default",{enumerable:!0,value:e})}:function(n,e){n.default=e}),__importStar=this&&this.__importStar||function(){var n=function(e){return n=Object.getOwnPropertyNames||function(n){var e=[];for(var t in n)Object.prototype.hasOwnProperty.call(n,t)&&(e[e.length]=t);return e},n(e)};return function(e){if(e&&e.__esModule)return e;var t={};if(null!=e)for(var o=n(e),a=0;a<o.length;a++)"default"!==o[a]&&__createBinding(t,e,o[a]);return __setModuleDefault(t,e),t}}();Object.defineProperty(exports,"__esModule",{value:!0}),exports.Grid3DWebviewProvider=void 0,exports.register3DGrid=register3DGrid;const vscode=__importStar(require("vscode")),nexus3d_cached_symbols_1=require("./nexus3d_cached_symbols"),nexus3d_error_index_1=require("./nexus3d_error_index"),nexus3d_scan_orchestrator_1=require("./nexus3d_scan_orchestrator"),nexus3d_world_adapter_1=require("./nexus3d_world_adapter");class Grid3DWebviewProvider{_extensionUri;api;static viewType="localhubGrid3D";_view;_panel;_nexusWorldAdapter;_nexusScanOrchestrator;_panelDisposables=[];_currentMode="classic";_nexusRefreshTimer;constructor(n,e,t){this._extensionUri=n,this.api=e,this._nexusWorldAdapter=new nexus3d_world_adapter_1.Nexus3DWorldAdapter(this.api),this._nexusScanOrchestrator=new nexus3d_scan_orchestrator_1.Nexus3DScanOrchestrator({cachedSymbolsProvider:new nexus3d_cached_symbols_1.Nexus3DCachedSymbols(this.api),errorIndex:t})}dispose(){this._clearNexusRefreshTimer(),this._disposePanelDisposables(),this._nexusScanOrchestrator.dispose()}async openFullscreen(){if(this._panel)return void this._panel.reveal();this._currentMode="classic",this._panel=vscode.window.createWebviewPanel("localhubGrid3D","🌌 LocalHub 3D Grid",vscode.ViewColumn.One,{enableScripts:!0,retainContextWhenHidden:!0,localResourceRoots:[this._extensionUri]});const n=await this._loadInitialData();this._panel.webview.html=this._getHtml(this._panel.webview,n),this._disposePanelDisposables(),this._panelDisposables.push(this._panel.webview.onDidReceiveMessage(async n=>{await this._handleMessage(n,this._panel.webview)})),this._panelDisposables.push(this._nexusScanOrchestrator.onDidUpdate(n=>{this._panel?.webview.postMessage({type:"nexusScanUpdate",data:n})})),this._panel.onDidDispose(()=>{this._disposePanelDisposables(),this._panel=void 0})}resolveWebviewView(n,e,t){this._view=n,n.webview.options={enableScripts:!0,localResourceRoots:[this._extensionUri]},n.webview.html=this._getCompactHtml(n.webview),n.webview.onDidReceiveMessage(async n=>{"openFullscreen"===n.type&&await this.openFullscreen()})}_disposePanelDisposables(){for(const n of this._panelDisposables)try{n.dispose()}catch{}this._panelDisposables=[]}_clearNexusRefreshTimer(){this._nexusRefreshTimer&&(clearTimeout(this._nexusRefreshTimer),this._nexusRefreshTimer=void 0)}_normalizePathKey(n){return"win32"===process.platform?n.toLowerCase():n}_isNexusModeActive(){return Boolean(this._panel)&&"nexus"===this._currentMode}async _loadClassicGraphData(){try{return await this.api.getFileRelationships(30,2)}catch(n){return console.error("Failed to load graph data:",n),{nodes:[],edges:[]}}}async _loadNexusWorldData(){try{return await this._nexusWorldAdapter.loadWorld()}catch(n){return console.error("Failed to load Nexus 3D world:",n),{nodes:[],edges:[],stats:{totalFiles:0,existingFiles:0,missingFiles:0,pendingCount:0,starredCount:0,relatedFiles:0,totalRelationships:0},loadedAt:Date.now()}}}async _loadInitialData(){const[n,e]=await Promise.all([this._loadClassicGraphData(),this._loadNexusWorldData()]);return{classic:n,nexus:e}}_primeNexusScans(n){const e=new Set(vscode.window.visibleTextEditors.filter(n=>"file"===n.document.uri.scheme).map(n=>this._normalizePathKey(n.document.uri.fsPath))),t=new Set(vscode.workspace.textDocuments.filter(n=>"file"===n.uri.scheme).map(n=>this._normalizePathKey(n.uri.fsPath))),o=n=>{const o=this._normalizePathKey(n.path);return e.has(o)||t.has(o)?4:n.isPending?3:n.isStarred?2:0};for(const a of[...n.nodes].sort((n,e)=>o(e)-o(n))){if(!a.exists)continue;const n=this._normalizePathKey(a.path);let o;e.has(n)||t.has(n)?o="visible":a.isPending?o="pending":a.isStarred&&(o="starred"),o?this._nexusScanOrchestrator.requestScan(a.path,o):this._nexusScanOrchestrator.requestScan(a.path,"background")}}handleDocumentActivity(n,e="visible"){this._isNexusModeActive()&&n&&(this._nexusScanOrchestrator.invalidate(n),this._nexusScanOrchestrator.requestScan(n,e))}scheduleNexusWorldRefresh(n=800){this._isNexusModeActive()&&(this._clearNexusRefreshTimer(),this._nexusRefreshTimer=setTimeout(async()=>{if(this._nexusRefreshTimer=void 0,!this._panel||"nexus"!==this._currentMode)return;const n=await this._loadNexusWorldData();this._primeNexusScans(n),this._panel.webview.postMessage({type:"updateNexusData",data:n})},n))}async _handleMessage(n,e){switch(n.type){case"openFile":const t=await vscode.workspace.openTextDocument(vscode.Uri.file(n.path)),o=await vscode.window.showTextDocument(t,vscode.ViewColumn.Beside);if("number"==typeof n.line){const e=Math.max(0,n.line-1),t=Math.max(0,("number"==typeof n.column?n.column:1)-1),a=new vscode.Position(e,t);o.selection=new vscode.Selection(a,a),o.revealRange(new vscode.Range(a,a),vscode.TextEditorRevealType.InCenter)}try{await vscode.commands.executeCommand("revealInExplorer",vscode.Uri.file(n.path))}catch{}break;case"refresh":if("nexus"===n.mode){this._currentMode="nexus";const n=await this._loadNexusWorldData();this._primeNexusScans(n),e.postMessage({type:"updateNexusData",data:n})}else{this._currentMode="classic";const n=await this._loadClassicGraphData();e.postMessage({type:"updateData",data:n})}break;case"switchMode":if(this._currentMode="nexus"===n.mode?"nexus":"classic","nexus"===n.mode){const n=await this._loadNexusWorldData();this._primeNexusScans(n),e.postMessage({type:"updateNexusData",data:n})}else{const n=await this._loadClassicGraphData();e.postMessage({type:"updateData",data:n})}break;case"requestNexusScan":"string"==typeof n.path&&n.path&&this._nexusScanOrchestrator.requestScan(n.path,n.priority??"focus",{force:Boolean(n.force)});break;case"log":console.log("[3D Grid]",n.message)}}_getCompactHtml(n){return'<!DOCTYPE html>\n<html>\n<head>\n    <style>\n        body {\n            padding: 20px;\n            display: flex;\n            flex-direction: column;\n            align-items: center;\n            justify-content: center;\n            height: 100vh;\n            margin: 0;\n            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);\n        }\n        .logo {\n            font-size: 64px;\n            margin-bottom: 20px;\n            animation: float 3s ease-in-out infinite;\n        }\n        @keyframes float {\n            0%, 100% { transform: translateY(0); }\n            50% { transform: translateY(-10px); }\n        }\n        button {\n            background: rgba(255,255,255,0.2);\n            border: 2px solid white;\n            color: white;\n            padding: 15px 30px;\n            font-size: 16px;\n            border-radius: 30px;\n            cursor: pointer;\n            transition: all 0.3s;\n        }\n        button:hover {\n            background: rgba(255,255,255,0.3);\n            transform: scale(1.05);\n        }\n        h2 {\n            color: white;\n            margin: 10px 0 30px 0;\n        }\n    </style>\n</head>\n<body>\n    <div class="logo">🌌</div>\n    <h2>3D Grid Visualization</h2>\n    <button onclick="openFullscreen()">Open 3D View</button>\n    <script>\n        const vscode = acquireVsCodeApi();\n        function openFullscreen() {\n            vscode.postMessage({ type: \'openFullscreen\' });\n        }\n    <\/script>\n</body>\n</html>'}_getHtml(n,e){const t=JSON.stringify(e);return`<!DOCTYPE html>\n<html>\n<head>\n    <meta charset="UTF-8">\n    <title>LocalHub 3D Grid</title>\n    <style>\n        ${this._getStyles()}\n    </style>\n</head>\n<body>\n    <div id="container"></div>\n    \n    \x3c!-- CONTROLS PANEL --\x3e\n    <div class="controls">\n        <div class="controls-header">\n            <span class="logo">🌌</span>\n            <span class="title">3D GRID</span>\n        </div>\n\n        <div class="control-group">\n            <label>🧭 Mode</label>\n            <div class="mode-switch">\n                <button class="mode-btn active" data-mode="classic" onclick="setMode('classic')">Classic</button>\n                <button class="mode-btn" data-mode="nexus" onclick="setMode('nexus')">Nexus 3D</button>\n            </div>\n        </div>\n        \n        \x3c!-- SHAPE SELECTOR --\x3e\n        <div class="control-group">\n            <label>🔮 Shape</label>\n            <div class="shape-buttons">\n                <button class="shape-btn active" data-shape="galaxy" onclick="setShape('galaxy')">\n                    <span class="icon">🌀</span>\n                    <span>Galaxy</span>\n                </button>\n                <button class="shape-btn" data-shape="sphere" onclick="setShape('sphere')">\n                    <span class="icon">🌐</span>\n                    <span>Sphere</span>\n                </button>\n                <button class="shape-btn" data-shape="cube" onclick="setShape('cube')">\n                    <span class="icon">📦</span>\n                    <span>Cube</span>\n                </button>\n                <button class="shape-btn" data-shape="helix" onclick="setShape('helix')">\n                    <span class="icon">🧬</span>\n                    <span>DNA</span>\n                </button>\n                <button class="shape-btn" data-shape="torus" onclick="setShape('torus')">\n                    <span class="icon">🍩</span>\n                    <span>Torus</span>\n                </button>\n                <button class="shape-btn" data-shape="wave" onclick="setShape('wave')">\n                    <span class="icon">🌊</span>\n                    <span>Wave</span>\n                </button>\n            </div>\n        </div>\n        \n        \x3c!-- COLOR SCHEME --\x3e\n        <div class="control-group">\n            <label>🎨 Colors</label>\n            <select id="colorScheme" onchange="setColorScheme(this.value)">\n                <option value="cosmos">🌌 Cosmos</option>\n                <option value="neon">💜 Neon</option>\n                <option value="matrix">💚 Matrix</option>\n                <option value="synthwave">🌅 Synthwave</option>\n                <option value="fire">🔥 Fire</option>\n                <option value="ice">❄️ Ice</option>\n                <option value="rainbow">🌈 Rainbow</option>\n            </select>\n        </div>\n        \n        \x3c!-- PHYSICS --\x3e\n        <div class="control-group">\n            <label>⚡ Physics</label>\n            <div class="physics-controls">\n                <label class="toggle">\n                    <input type="checkbox" id="gravity" checked onchange="toggleGravity()">\n                    <span>Gravity</span>\n                </label>\n                <label class="toggle">\n                    <input type="checkbox" id="rotation" checked onchange="toggleRotation()">\n                    <span>Auto-Rotate</span>\n                </label>\n                <label class="toggle">\n                    <input type="checkbox" id="particles" checked onchange="toggleParticles()">\n                    <span>Particles</span>\n                </label>\n            </div>\n        </div>\n        \n        \x3c!-- SPEED CONTROLS --\x3e\n        <div class="control-group">\n            <label>🚀 Speed</label>\n            <input type="range" id="speed" min="0" max="200" value="100" \n                   oninput="setSpeed(this.value)">\n            <span id="speedValue">100%</span>\n        </div>\n        \n        \x3c!-- ZOOM --\x3e\n        <div class="control-group">\n            <label>🔍 Zoom</label>\n            <input type="range" id="zoom" min="50" max="200" value="100"\n                   oninput="setZoom(this.value)">\n        </div>\n        \n        \x3c!-- FILTERS --\x3e\n        <div class="control-group">\n            <label>🔗 Connections</label>\n            <div class="filter-controls">\n                <label class="toggle">\n                    <input type="checkbox" checked onchange="toggleEdgeType('group')">\n                    <span class="dot group"></span>\n                    <span>Groups</span>\n                </label>\n                <label class="toggle">\n                    <input type="checkbox" checked onchange="toggleEdgeType('temporal')">\n                    <span class="dot temporal"></span>\n                    <span>Temporal</span>\n                </label>\n            </div>\n        </div>\n        \n        \x3c!-- ACTION BUTTONS --\x3e\n        <div class="action-buttons">\n            <button class="action-btn" onclick="resetCamera()">\n                <span>📷</span> Reset View\n            </button>\n            <button class="action-btn" onclick="explodeView()">\n                <span>💥</span> Explode\n            </button>\n            <button class="action-btn" onclick="implodeView()">\n                <span>🌀</span> Implode\n            </button>\n            <button class="action-btn" onclick="refreshData()">\n                <span>🔄</span> Refresh\n            </button>\n        </div>\n\n        \x3c!-- COSMIC EFFECTS --\x3e\n        <div class="control-group">\n            <label>🌌 Cosmic Effects</label>\n            <div class="cosmic-buttons">\n                <button class="cosmic-btn" onclick="testBlackHole()">\n                    <span>🕳️</span>\n                    <span>Black Hole</span>\n                </button>\n                <button class="cosmic-btn" onclick="testSupernova()">\n                    <span>💥</span>\n                    <span>Supernova</span>\n                </button>\n                <button class="cosmic-btn" onclick="testWormhole()">\n                    <span>🌀</span>\n                    <span>Wormhole</span>\n                </button>\n            </div>\n        </div>\n    </div>\n    \n    \x3c!-- INFO PANEL --\x3e\n    <div class="info-panel" id="infoPanel">\n        <div class="info-header">\n            <span id="infoTitle">Select a node</span>\n            <button class="close-btn" onclick="closeInfo()">×</button>\n        </div>\n        <div class="info-content" id="infoContent"></div>\n    </div>\n    \n    \x3c!-- STATS --\x3e\n    <div class="stats">\n        <div class="stat">\n            <span class="stat-value" id="nodeCount">0</span>\n            <span class="stat-label">Nodes</span>\n        </div>\n        <div class="stat">\n            <span class="stat-value" id="edgeCount">0</span>\n            <span class="stat-label">Edges</span>\n        </div>\n        <div class="stat">\n            <span class="stat-value" id="fps">60</span>\n            <span class="stat-label">FPS</span>\n        </div>\n    </div>\n    \n    \x3c!-- LOADING --\x3e\n    <div class="loading" id="loading">\n        <div class="loading-spinner"></div>\n        <div>Initializing 3D Universe...</div>\n    </div>\n\n    \x3c!-- THREE.JS --\x3e\n    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"><\/script>\n    \n    <script>\n        // Initial data from backend\n        const initialPayload = ${t};\n        \n        ${this._getScript()}\n    <\/script>\n</body>\n</html>`}_getStyles(){return'\n/* ═══════════════════════════════════════════════════════════════════════════\n   🎨 COSMIC STYLES\n   ═══════════════════════════════════════════════════════════════════════════ */\n\n* {\n    margin: 0;\n    padding: 0;\n    box-sizing: border-box;\n}\n\nbody {\n    font-family: -apple-system, BlinkMacSystemFont, \'Segoe UI\', Roboto, sans-serif;\n    overflow: hidden;\n    background: #000;\n    color: #fff;\n    user-select: none;\n}\n\n#container {\n    position: absolute;\n    top: 0;\n    left: 0;\n    width: 100%;\n    height: 100%;\n}\n\n/* ═══════════════════════════════════════════════════════════════════════════\n   CONTROLS PANEL\n   ═══════════════════════════════════════════════════════════════════════════ */\n\n.controls {\n    position: absolute;\n    left: 20px;\n    top: 20px;\n    width: 280px;\n    background: linear-gradient(135deg, \n        rgba(20, 20, 40, 0.95) 0%, \n        rgba(30, 20, 60, 0.95) 100%);\n    border: 1px solid rgba(138, 43, 226, 0.3);\n    border-radius: 20px;\n    padding: 20px;\n    backdrop-filter: blur(20px);\n    box-shadow: 0 8px 32px rgba(138, 43, 226, 0.3),\n                inset 0 1px 0 rgba(255,255,255,0.1);\n    z-index: 100;\n    max-height: calc(100vh - 40px);\n    overflow-y: auto;\n}\n\n.controls::-webkit-scrollbar {\n    width: 6px;\n}\n\n.controls::-webkit-scrollbar-track {\n    background: rgba(255,255,255,0.05);\n    border-radius: 3px;\n}\n\n.controls::-webkit-scrollbar-thumb {\n    background: rgba(138, 43, 226, 0.5);\n    border-radius: 3px;\n}\n\n.controls-header {\n    display: flex;\n    align-items: center;\n    gap: 10px;\n    margin-bottom: 20px;\n    padding-bottom: 15px;\n    border-bottom: 1px solid rgba(138, 43, 226, 0.2);\n}\n\n.controls-header .logo {\n    font-size: 28px;\n    filter: drop-shadow(0 0 10px rgba(138, 43, 226, 0.8));\n}\n\n.controls-header .title {\n    font-size: 18px;\n    font-weight: 700;\n    background: linear-gradient(135deg, #667eea 0%, #f093fb 100%);\n    -webkit-background-clip: text;\n    -webkit-text-fill-color: transparent;\n    letter-spacing: 2px;\n}\n\n/* Control Groups */\n.control-group {\n    margin-bottom: 20px;\n}\n\n.control-group label {\n    display: block;\n    font-size: 11px;\n    font-weight: 600;\n    text-transform: uppercase;\n    letter-spacing: 1px;\n    margin-bottom: 8px;\n    color: rgba(255,255,255,0.6);\n}\n\n.mode-switch {\n    display: grid;\n    grid-template-columns: 1fr 1fr;\n    gap: 8px;\n}\n\n.mode-btn {\n    background: rgba(255,255,255,0.05);\n    border: 1px solid rgba(138, 43, 226, 0.2);\n    border-radius: 10px;\n    padding: 10px 12px;\n    color: rgba(255,255,255,0.7);\n    cursor: pointer;\n    transition: all 0.25s;\n    font-size: 12px;\n    font-weight: 600;\n}\n\n.mode-btn:hover {\n    background: rgba(138, 43, 226, 0.18);\n    border-color: rgba(138, 43, 226, 0.45);\n}\n\n.mode-btn.active {\n    background: linear-gradient(135deg, rgba(102, 126, 234, 0.3), rgba(240, 147, 251, 0.3));\n    border-color: rgba(240, 147, 251, 0.8);\n    color: white;\n    box-shadow: 0 4px 12px rgba(138, 43, 226, 0.3);\n}\n\n/* Shape Buttons */\n.shape-buttons {\n    display: grid;\n    grid-template-columns: repeat(3, 1fr);\n    gap: 8px;\n}\n\n.shape-btn {\n    background: rgba(255,255,255,0.05);\n    border: 1px solid rgba(138, 43, 226, 0.2);\n    border-radius: 12px;\n    padding: 12px 8px;\n    color: rgba(255,255,255,0.6);\n    cursor: pointer;\n    transition: all 0.3s;\n    display: flex;\n    flex-direction: column;\n    align-items: center;\n    gap: 4px;\n}\n\n.shape-btn:hover {\n    background: rgba(138, 43, 226, 0.2);\n    border-color: rgba(138, 43, 226, 0.5);\n    transform: translateY(-2px);\n}\n\n.shape-btn.active {\n    background: linear-gradient(135deg, rgba(138, 43, 226, 0.3), rgba(255, 0, 128, 0.3));\n    border-color: rgba(138, 43, 226, 0.8);\n    color: white;\n    box-shadow: 0 4px 12px rgba(138, 43, 226, 0.4);\n}\n\n.shape-btn .icon {\n    font-size: 20px;\n}\n\n.shape-btn span:last-child {\n    font-size: 10px;\n    font-weight: 500;\n}\n\n/* Select */\nselect {\n    width: 100%;\n    background: rgba(255,255,255,0.05);\n    border: 1px solid rgba(138, 43, 226, 0.3);\n    border-radius: 8px;\n    padding: 10px;\n    color: white;\n    font-size: 13px;\n    cursor: pointer;\n}\n\nselect option {\n    background: #1a1a2e;\n}\n\n/* Toggles */\n.physics-controls, .filter-controls {\n    display: flex;\n    flex-direction: column;\n    gap: 8px;\n}\n\n.toggle {\n    display: flex;\n    align-items: center;\n    gap: 8px;\n    cursor: pointer;\n    padding: 8px;\n    border-radius: 8px;\n    transition: background 0.2s;\n}\n\n.toggle:hover {\n    background: rgba(255,255,255,0.05);\n}\n\n.toggle input {\n    width: 16px;\n    height: 16px;\n    cursor: pointer;\n}\n\n.toggle .dot {\n    width: 10px;\n    height: 10px;\n    border-radius: 50%;\n}\n\n.dot.group {\n    background: #66bb6a;\n}\n\n.dot.temporal {\n    background: #ffa726;\n}\n\n/* Range Inputs */\ninput[type="range"] {\n    width: 100%;\n    height: 6px;\n    background: rgba(255,255,255,0.1);\n    border-radius: 3px;\n    outline: none;\n    -webkit-appearance: none;\n}\n\ninput[type="range"]::-webkit-slider-thumb {\n    -webkit-appearance: none;\n    width: 16px;\n    height: 16px;\n    background: linear-gradient(135deg, #667eea, #f093fb);\n    border-radius: 50%;\n    cursor: pointer;\n    box-shadow: 0 2px 8px rgba(138, 43, 226, 0.4);\n}\n\n#speedValue {\n    display: inline-block;\n    margin-left: 10px;\n    font-size: 12px;\n    color: rgba(255,255,255,0.6);\n}\n\n/* Action Buttons */\n.action-buttons {\n    display: grid;\n    grid-template-columns: 1fr 1fr;\n    gap: 8px;\n    margin-top: 20px;\n    padding-top: 20px;\n    border-top: 1px solid rgba(138, 43, 226, 0.2);\n}\n\n.action-btn {\n    background: linear-gradient(135deg, rgba(138, 43, 226, 0.2), rgba(255, 0, 128, 0.2));\n    border: 1px solid rgba(138, 43, 226, 0.3);\n    border-radius: 10px;\n    padding: 10px;\n    color: white;\n    font-size: 12px;\n    cursor: pointer;\n    transition: all 0.3s;\n    display: flex;\n    align-items: center;\n    justify-content: center;\n    gap: 6px;\n}\n\n.action-btn:hover {\n    background: linear-gradient(135deg, rgba(138, 43, 226, 0.4), rgba(255, 0, 128, 0.4));\n    transform: translateY(-2px);\n    box-shadow: 0 4px 12px rgba(138, 43, 226, 0.4);\n}\n\n/* Cosmic Effects Buttons */\n.cosmic-buttons {\n    display: grid;\n    grid-template-columns: repeat(3, 1fr);\n    gap: 8px;\n    margin-top: 10px;\n}\n\n.cosmic-btn {\n    background: linear-gradient(135deg,\n        rgba(139, 0, 139, 0.3),\n        rgba(255, 0, 128, 0.3));\n    border: 1px solid rgba(255, 0, 255, 0.5);\n    border-radius: 12px;\n    padding: 10px;\n    color: rgba(255,255,255,0.9);\n    cursor: pointer;\n    transition: all 0.3s;\n    display: flex;\n    flex-direction: column;\n    align-items: center;\n    gap: 4px;\n}\n\n.cosmic-btn:hover {\n    background: linear-gradient(135deg,\n        rgba(139, 0, 139, 0.5),\n        rgba(255, 0, 128, 0.5));\n    transform: scale(1.05);\n    box-shadow: 0 0 20px rgba(255, 0, 255, 0.5);\n}\n\n.cosmic-btn span:first-child {\n    font-size: 24px;\n    filter: drop-shadow(0 0 5px currentColor);\n}\n\n.cosmic-btn span:last-child {\n    font-size: 10px;\n    font-weight: 600;\n    text-transform: uppercase;\n    letter-spacing: 1px;\n}\n\n/* ═══════════════════════════════════════════════════════════════════════════\n   INFO PANEL\n   ═══════════════════════════════════════════════════════════════════════════ */\n\n.info-panel {\n    position: absolute;\n    right: 20px;\n    top: 20px;\n    width: 320px;\n    background: linear-gradient(135deg, \n        rgba(20, 20, 40, 0.95) 0%, \n        rgba(40, 20, 60, 0.95) 100%);\n    border: 1px solid rgba(138, 43, 226, 0.3);\n    border-radius: 20px;\n    padding: 20px;\n    backdrop-filter: blur(20px);\n    box-shadow: 0 8px 32px rgba(138, 43, 226, 0.3);\n    z-index: 100;\n    display: none;\n    animation: slideIn 0.3s ease;\n}\n\n@keyframes slideIn {\n    from {\n        transform: translateX(20px);\n        opacity: 0;\n    }\n}\n\n.info-panel.show {\n    display: block;\n}\n\n.info-header {\n    display: flex;\n    justify-content: space-between;\n    align-items: center;\n    margin-bottom: 15px;\n    padding-bottom: 10px;\n    border-bottom: 1px solid rgba(138, 43, 226, 0.2);\n}\n\n#infoTitle {\n    font-weight: 600;\n    font-size: 14px;\n}\n\n.close-btn {\n    background: none;\n    border: none;\n    color: rgba(255,255,255,0.5);\n    font-size: 24px;\n    cursor: pointer;\n    padding: 0;\n    width: 24px;\n    height: 24px;\n    display: flex;\n    align-items: center;\n    justify-content: center;\n    border-radius: 50%;\n    transition: all 0.2s;\n}\n\n.close-btn:hover {\n    background: rgba(255,255,255,0.1);\n    color: white;\n}\n\n.info-content {\n    font-size: 12px;\n    line-height: 1.6;\n    color: rgba(255,255,255,0.8);\n}\n\n.info-item {\n    margin-bottom: 10px;\n    padding: 8px;\n    background: rgba(255,255,255,0.05);\n    border-radius: 8px;\n}\n\n.info-label {\n    font-weight: 600;\n    color: rgba(255,255,255,0.6);\n    display: block;\n    margin-bottom: 2px;\n    font-size: 10px;\n    text-transform: uppercase;\n    letter-spacing: 0.5px;\n}\n\n.info-value {\n    color: white;\n}\n\n.info-value.path {\n    word-break: break-all;\n}\n\n.info-badges {\n    display: flex;\n    flex-wrap: wrap;\n    gap: 6px;\n    margin-bottom: 12px;\n}\n\n.info-badge {\n    display: inline-flex;\n    align-items: center;\n    gap: 4px;\n    padding: 4px 8px;\n    border-radius: 999px;\n    font-size: 10px;\n    font-weight: 700;\n    text-transform: uppercase;\n    letter-spacing: 0.6px;\n    background: rgba(255,255,255,0.08);\n    color: rgba(255,255,255,0.88);\n}\n\n.info-badge.pending {\n    background: rgba(58, 134, 255, 0.22);\n    color: #8dc7ff;\n}\n\n.info-badge.starred {\n    background: rgba(255, 190, 11, 0.22);\n    color: #ffd76b;\n}\n\n.info-badge.error {\n    background: rgba(255, 0, 110, 0.22);\n    color: #ff8db8;\n}\n\n.info-badge.warning {\n    background: rgba(255, 190, 11, 0.18);\n    color: #ffe299;\n}\n\n.info-badge.active {\n    background: rgba(251, 86, 7, 0.2);\n    color: #ffb289;\n}\n\n.info-section-title {\n    margin: 14px 0 8px 0;\n    font-size: 11px;\n    font-weight: 700;\n    color: rgba(255,255,255,0.72);\n    text-transform: uppercase;\n    letter-spacing: 1px;\n}\n\n.info-list {\n    display: flex;\n    flex-direction: column;\n    gap: 8px;\n}\n\n.info-list-item {\n    padding: 8px;\n    border-radius: 10px;\n    background: rgba(255,255,255,0.05);\n    border: 1px solid rgba(255,255,255,0.04);\n}\n\n.info-list-item.clickable {\n    cursor: pointer;\n    transition: background 0.2s, border-color 0.2s;\n}\n\n.info-list-item.clickable:hover {\n    background: rgba(138, 43, 226, 0.16);\n    border-color: rgba(138, 43, 226, 0.35);\n}\n\n.info-list-meta {\n    display: flex;\n    justify-content: space-between;\n    gap: 8px;\n    margin-bottom: 4px;\n    color: rgba(255,255,255,0.56);\n    font-size: 10px;\n    text-transform: uppercase;\n    letter-spacing: 0.5px;\n}\n\n.info-actions {\n    display: flex;\n    gap: 8px;\n    margin-top: 12px;\n}\n\n.info-action-btn {\n    flex: 1;\n    background: linear-gradient(135deg, rgba(102, 126, 234, 0.24), rgba(240, 147, 251, 0.24));\n    border: 1px solid rgba(138, 43, 226, 0.35);\n    border-radius: 10px;\n    padding: 10px;\n    color: white;\n    cursor: pointer;\n    font-size: 12px;\n    font-weight: 600;\n    transition: all 0.2s;\n}\n\n.info-action-btn:hover {\n    transform: translateY(-1px);\n    box-shadow: 0 4px 14px rgba(138, 43, 226, 0.28);\n}\n\n/* ═══════════════════════════════════════════════════════════════════════════\n   STATS\n   ═══════════════════════════════════════════════════════════════════════════ */\n\n.stats {\n    position: absolute;\n    bottom: 20px;\n    left: 20px;\n    display: flex;\n    gap: 20px;\n    background: rgba(20, 20, 40, 0.9);\n    padding: 15px 20px;\n    border-radius: 15px;\n    backdrop-filter: blur(10px);\n    border: 1px solid rgba(138, 43, 226, 0.2);\n}\n\n.stat {\n    display: flex;\n    flex-direction: column;\n    align-items: center;\n    gap: 4px;\n}\n\n.stat-value {\n    font-size: 18px;\n    font-weight: 700;\n    background: linear-gradient(135deg, #667eea, #f093fb);\n    -webkit-background-clip: text;\n    -webkit-text-fill-color: transparent;\n}\n\n.stat-label {\n    font-size: 10px;\n    text-transform: uppercase;\n    letter-spacing: 1px;\n    color: rgba(255,255,255,0.5);\n}\n\n/* ═══════════════════════════════════════════════════════════════════════════\n   LOADING\n   ═══════════════════════════════════════════════════════════════════════════ */\n\n.loading {\n    position: fixed;\n    top: 0;\n    left: 0;\n    right: 0;\n    bottom: 0;\n    background: radial-gradient(circle at center, \n        rgba(138, 43, 226, 0.2) 0%, \n        rgba(0, 0, 0, 0.95) 100%);\n    display: flex;\n    flex-direction: column;\n    justify-content: center;\n    align-items: center;\n    gap: 20px;\n    z-index: 1000;\n    font-size: 14px;\n    color: rgba(255,255,255,0.8);\n}\n\n.loading.hidden {\n    display: none;\n}\n\n.loading-spinner {\n    width: 60px;\n    height: 60px;\n    border: 3px solid rgba(138, 43, 226, 0.2);\n    border-top-color: #8a2be2;\n    border-radius: 50%;\n    animation: spin 1s linear infinite;\n}\n\n@keyframes spin {\n    to { transform: rotate(360deg); }\n}\n'}_getScript(){return"\n// ═══════════════════════════════════════════════════════════════════════════\n// 🌌 3D GRID VISUALIZATION ENGINE\n// ═══════════════════════════════════════════════════════════════════════════\n\nconst vscode = acquireVsCodeApi();\n\n// ─────────────────────────────────────────────────────────────────────────\n// GLOBALS\n// ─────────────────────────────────────────────────────────────────────────\n\nconst classicData = initialPayload.classic || { nodes: [], edges: [] };\nlet nexusData = initialPayload.nexus || { nodes: [], edges: [], stats: {} };\n\nlet scene, camera, renderer, controls;\nlet nodes = new Map();\nlet edges = [];\nlet particles;\nlet currentMode = 'classic';\nlet currentData = classicData;\nlet currentShape = 'galaxy';\nlet currentColorScheme = 'cosmos';\nlet autoRotate = true;\nlet gravityEnabled = true;\nlet particlesEnabled = true;\nlet animationSpeed = 1;\nlet edgeFilters = { group: true, temporal: true };\nlet selectedNodeId = null;\nlet selectedNodeData = null;\nlet nexusScanResults = new Map();\nlet hoverScanTimer = null;\nlet hoveredNodeId = null;\n\n// Cosmic effects\nlet blackHoles = [];\nlet supernovas = [];\nlet wormholes = [];\n\n// Color schemes\nconst COLOR_SCHEMES = {\n    cosmos: {\n        bg: 0x000814,\n        node: [0x667eea, 0xf093fb, 0x00d4ff],\n        edge: { group: 0x66bb6a, temporal: 0xffa726 },\n        particle: 0x8a2be2\n    },\n    neon: {\n        bg: 0x0a0a0a,\n        node: [0xff006e, 0x8338ec, 0x3a86ff],\n        edge: { group: 0x06ffa5, temporal: 0xffbe0b },\n        particle: 0xff006e\n    },\n    matrix: {\n        bg: 0x000000,\n        node: [0x00ff00, 0x00cc00, 0x009900],\n        edge: { group: 0x00ff00, temporal: 0x00ff88 },\n        particle: 0x00ff00\n    },\n    synthwave: {\n        bg: 0x1a0033,\n        node: [0xff006e, 0xfb5607, 0xffbe0b],\n        edge: { group: 0x8338ec, temporal: 0x3a86ff },\n        particle: 0xff71ce\n    },\n    fire: {\n        bg: 0x0f0000,\n        node: [0xff4500, 0xff6347, 0xffa500],\n        edge: { group: 0xffff00, temporal: 0xff8c00 },\n        particle: 0xff4500\n    },\n    ice: {\n        bg: 0x001233,\n        node: [0x00b4d8, 0x90e0ef, 0xcaf0f8],\n        edge: { group: 0x48cae4, temporal: 0xade8f4 },\n        particle: 0x00b4d8\n    },\n    rainbow: {\n        bg: 0x0a0a0a,\n        node: [0xff0000, 0x00ff00, 0x0000ff, 0xffff00, 0xff00ff, 0x00ffff],\n        edge: { group: 0xff00ff, temporal: 0x00ff00 },\n        particle: 0xffffff\n    }\n};\n\n// ─────────────────────────────────────────────────────────────────────────\n// INITIALIZATION\n// ─────────────────────────────────────────────────────────────────────────\n\nfunction init() {\n    // Scene\n    scene = new THREE.Scene();\n    scene.background = new THREE.Color(COLOR_SCHEMES.cosmos.bg);\n    scene.fog = new THREE.FogExp2(COLOR_SCHEMES.cosmos.bg, 0.001);\n    \n    // Camera\n    camera = new THREE.PerspectiveCamera(\n        60, window.innerWidth / window.innerHeight, 1, 10000\n    );\n    camera.position.set(0, 0, 500);\n    \n    // Renderer\n    renderer = new THREE.WebGLRenderer({ antialias: true });\n    renderer.setSize(window.innerWidth, window.innerHeight);\n    renderer.setPixelRatio(window.devicePixelRatio);\n    document.getElementById('container').appendChild(renderer.domElement);\n    \n    // Controls\n    setupControls();\n    \n    // Lights\n    setupLights();\n    \n    // Create visualization\n    createVisualization(classicData);\n    \n    // Particles\n    createParticles();\n    \n    // Events\n    window.addEventListener('resize', onWindowResize);\n    renderer.domElement.addEventListener('click', onNodeClick);\n    renderer.domElement.addEventListener('mousemove', onNodeHover);\n    \n    // Hide loading\n    setTimeout(() => {\n        document.getElementById('loading').classList.add('hidden');\n    }, 500);\n    \n    // Start animation\n    animate();\n}\n\nfunction getNodeId(data) {\n    return data.id;\n}\n\nfunction getNodePath(data) {\n    return data.full_path || data.path || data.id;\n}\n\nfunction getNodeSize(data) {\n    return data.graphSize || data.size || 1;\n}\n\nfunction resolveHeatColor(heat) {\n    if (heat >= 0.85) return 0xff335e;\n    if (heat >= 0.6) return 0xff7b54;\n    if (heat >= 0.3) return 0xffbe0b;\n    return null;\n}\n\nfunction resolveNodeColor(data) {\n    if (currentMode === 'nexus') {\n        if (data.isPending) return 0x3a86ff;\n        if (data.isStarred) return 0xffbe0b;\n        const heatColor = resolveHeatColor(data.heat || 0);\n        if (heatColor !== null) {\n            return heatColor;\n        }\n    }\n\n    const colors = COLOR_SCHEMES[currentColorScheme].node;\n    return colors[Math.floor(Math.random() * colors.length)];\n}\n\nfunction escapeHtml(value) {\n    return String(value ?? '')\n        .replace(/&/g, '&amp;')\n        .replace(/</g, '&lt;')\n        .replace(/>/g, '&gt;')\n        .replace(/\"/g, '&quot;')\n        .replace(/'/g, '&#39;');\n}\n\nfunction formatTimestamp(seconds) {\n    if (!seconds || seconds <= 0) {\n        return 'n/a';\n    }\n    try {\n        return new Date(seconds * 1000).toLocaleString();\n    } catch {\n        return 'n/a';\n    }\n}\n\nfunction formatScanTimestamp(ms) {\n    if (!ms || ms <= 0) {\n        return 'not scanned yet';\n    }\n    try {\n        return new Date(ms).toLocaleString();\n    } catch {\n        return 'not scanned yet';\n    }\n}\n\n// ─────────────────────────────────────────────────────────────────────────\n// CONTROLS\n// ─────────────────────────────────────────────────────────────────────────\n\nfunction setupControls() {\n    // Simple orbit controls simulation\n    let isMouseDown = false;\n    let mouseX = 0;\n    let mouseY = 0;\n    let targetRotationX = 0;\n    let targetRotationY = 0;\n    \n    renderer.domElement.addEventListener('mousedown', (e) => {\n        if (e.ctrlKey || e.button === 1) { // Ctrl + LMB or Middle mouse\n            isMouseDown = true;\n            mouseX = e.clientX;\n            mouseY = e.clientY;\n        }\n    });\n    \n    renderer.domElement.addEventListener('mousemove', (e) => {\n        if (isMouseDown) {\n            const deltaX = e.clientX - mouseX;\n            const deltaY = e.clientY - mouseY;\n            \n            targetRotationY += deltaX * 0.01;\n            targetRotationX += deltaY * 0.01;\n            \n            mouseX = e.clientX;\n            mouseY = e.clientY;\n        }\n    });\n    \n    renderer.domElement.addEventListener('mouseup', () => {\n        isMouseDown = false;\n    });\n    \n    // Mouse wheel zoom\n    renderer.domElement.addEventListener('wheel', (e) => {\n        const delta = e.deltaY * 0.5;\n        camera.position.z = Math.max(100, Math.min(1500, camera.position.z + delta));\n    });\n}\n\n// ─────────────────────────────────────────────────────────────────────────\n// LIGHTS\n// ─────────────────────────────────────────────────────────────────────────\n\nfunction setupLights() {\n    // Ambient light\n    const ambientLight = new THREE.AmbientLight(0xffffff, 0.3);\n    scene.add(ambientLight);\n    \n    // Directional lights\n    const dirLight1 = new THREE.DirectionalLight(0x667eea, 0.5);\n    dirLight1.position.set(1, 1, 1);\n    scene.add(dirLight1);\n    \n    const dirLight2 = new THREE.DirectionalLight(0xf093fb, 0.5);\n    dirLight2.position.set(-1, -1, 1);\n    scene.add(dirLight2);\n    \n    // Point light at center\n    const pointLight = new THREE.PointLight(0x8a2be2, 1, 1000);\n    pointLight.position.set(0, 0, 0);\n    scene.add(pointLight);\n}\n\n// ─────────────────────────────────────────────────────────────────────────\n// VISUALIZATION\n// ─────────────────────────────────────────────────────────────────────────\n\nfunction createVisualization(data) {\n    if (!data || !data.nodes) return;\n    currentData = data;\n    \n    // Clear existing\n    clearVisualization();\n    \n    // Create nodes\n    const nodeCount = data.nodes.length;\n    const positions = getShapePositions(currentShape, nodeCount);\n    \n    data.nodes.forEach((nodeData, i) => {\n        const node = createNode(nodeData, positions[i]);\n        nodes.set(nodeData.id, node);\n        scene.add(node.mesh);\n    });\n    \n    // Create edges\n    if (data.edges) {\n        data.edges.forEach(edgeData => {\n            const edge = createEdge(edgeData);\n            if (edge) {\n                edges.push(edge);\n                scene.add(edge.line);\n            }\n        });\n    }\n    \n    // Update stats\n    updateStats();\n\n    if (selectedNodeId) {\n        const refreshedNode = nodes.get(selectedNodeId);\n        if (!refreshedNode) {\n            selectedNodeId = null;\n            selectedNodeData = null;\n            closeInfo();\n        } else {\n            selectedNodeData = refreshedNode.data;\n            showNodeInfo(refreshedNode.data);\n        }\n    }\n}\n\nfunction createNode(data, position) {\n    // Geometry\n    const baseSize = getNodeSize(data);\n    const heatBoost = currentMode === 'nexus' ? (data.heat || 0) * 6 : 0;\n    const geometry = new THREE.SphereGeometry(5 + baseSize * 0.1 + heatBoost, 16, 16);\n    \n    // Material\n    const color = resolveNodeColor(data);\n    \n    const material = new THREE.MeshPhongMaterial({\n        color: color,\n        emissive: color,\n        emissiveIntensity: currentMode === 'nexus' ? 0.2 + Math.min(0.35, (data.heat || 0) * 0.45) : 0.3,\n        shininess: 100,\n        transparent: true,\n        opacity: 0.9\n    });\n    \n    // Mesh\n    const mesh = new THREE.Mesh(geometry, material);\n    mesh.position.copy(position);\n    mesh.userData = data;\n    \n    // Add glow\n    const glowGeometry = new THREE.SphereGeometry(7 + baseSize * 0.1 + heatBoost, 8, 8);\n    const glowMaterial = new THREE.MeshBasicMaterial({\n        color: color,\n        transparent: true,\n        opacity: 0.2\n    });\n    const glow = new THREE.Mesh(glowGeometry, glowMaterial);\n    mesh.add(glow);\n\n    let halo = null;\n    if (currentMode === 'nexus' && data.isStarred) {\n        const haloGeometry = new THREE.TorusGeometry(10 + baseSize * 0.1 + heatBoost, 1.2, 10, 48);\n        const haloMaterial = new THREE.MeshBasicMaterial({\n            color: 0xffd166,\n            transparent: true,\n            opacity: 0.65\n        });\n        halo = new THREE.Mesh(haloGeometry, haloMaterial);\n        halo.rotation.x = Math.PI / 2;\n        mesh.add(halo);\n    }\n\n    let outline = null;\n    if (currentMode === 'nexus' && data.isPending) {\n        const outlineGeometry = new THREE.SphereGeometry(8.5 + baseSize * 0.1 + heatBoost, 14, 14);\n        const outlineMaterial = new THREE.MeshBasicMaterial({\n            color: 0x3a86ff,\n            transparent: true,\n            opacity: 0.18,\n            wireframe: true\n        });\n        outline = new THREE.Mesh(outlineGeometry, outlineMaterial);\n        mesh.add(outline);\n    }\n    \n    // Physics\n    mesh.userData.velocity = new THREE.Vector3(\n        (Math.random() - 0.5) * 0.1,\n        (Math.random() - 0.5) * 0.1,\n        (Math.random() - 0.5) * 0.1\n    );\n    \n    return {\n        mesh,\n        data,\n        position,\n        originalPosition: position.clone(),\n        glow,\n        halo,\n        outline,\n        errorAura: null,\n        pulseOffset: Math.random() * Math.PI * 2\n    };\n}\n\nfunction createEdge(data) {\n    const source = nodes.get(data.source);\n    const target = nodes.get(data.target);\n    \n    if (!source || !target) return null;\n    \n    // Geometry\n    const geometry = new THREE.BufferGeometry();\n    const positions = new Float32Array(6);\n    positions[0] = source.position.x;\n    positions[1] = source.position.y;\n    positions[2] = source.position.z;\n    positions[3] = target.position.x;\n    positions[4] = target.position.y;\n    positions[5] = target.position.z;\n    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));\n    \n    // Material\n    const color = COLOR_SCHEMES[currentColorScheme].edge[data.type] || 0xffffff;\n    const material = new THREE.LineBasicMaterial({\n        color: color,\n        transparent: true,\n        opacity: 0.3 + data.weight * 0.1,\n        linewidth: 1 + data.weight * 0.5\n    });\n    \n    // Line\n    const line = new THREE.Line(geometry, material);\n    line.userData = data;\n    \n    return {\n        line,\n        source,\n        target,\n        data\n    };\n}\n\n// ─────────────────────────────────────────────────────────────────────────\n// PARTICLES\n// ─────────────────────────────────────────────────────────────────────────\n\nfunction createParticles() {\n    const particleCount = 1000;\n    const geometry = new THREE.BufferGeometry();\n    const positions = new Float32Array(particleCount * 3);\n    const colors = new Float32Array(particleCount * 3);\n    \n    for (let i = 0; i < particleCount; i++) {\n        const i3 = i * 3;\n        positions[i3] = (Math.random() - 0.5) * 2000;\n        positions[i3 + 1] = (Math.random() - 0.5) * 2000;\n        positions[i3 + 2] = (Math.random() - 0.5) * 2000;\n        \n        const color = new THREE.Color(COLOR_SCHEMES[currentColorScheme].particle);\n        colors[i3] = color.r;\n        colors[i3 + 1] = color.g;\n        colors[i3 + 2] = color.b;\n    }\n    \n    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));\n    geometry.setAttribute('color', new THREE.BufferAttribute(colors, 3));\n    \n    const material = new THREE.PointsMaterial({\n        size: 2,\n        vertexColors: true,\n        transparent: true,\n        opacity: 0.6,\n        blending: THREE.AdditiveBlending\n    });\n    \n    particles = new THREE.Points(geometry, material);\n    scene.add(particles);\n}\n\n// ─────────────────────────────────────────────────────────────────────────\n// 🕳️ BLACK HOLES\n// ─────────────────────────────────────────────────────────────────────────\n\nfunction createBlackHole(position, nodeName) {\n    var bhGroup = new THREE.Group();\n\n    // Горизонт событий\n    var holeGeo = new THREE.SphereGeometry(15, 32, 32);\n    var holeMat = new THREE.MeshBasicMaterial({ color: 0x000000, side: THREE.BackSide });\n    bhGroup.add(new THREE.Mesh(holeGeo, holeMat));\n\n    // Аккреционный диск\n    var diskGeo = new THREE.TorusGeometry(25, 5, 8, 100);\n    var diskMat = new THREE.MeshBasicMaterial({\n        color: 0xff4400, transparent: true, opacity: 0.8\n    });\n    var disk = new THREE.Mesh(diskGeo, diskMat);\n    disk.rotation.x = Math.PI / 2;\n    bhGroup.add(disk);\n\n    // Частицы спирали\n    var pCount = 500;\n    var geo = new THREE.BufferGeometry();\n    var pos = new Float32Array(pCount * 3);\n    var col = new Float32Array(pCount * 3);\n    for (var i = 0; i < pCount; i++) {\n        var angle = Math.random() * Math.PI * 2;\n        var r = 50 + Math.random() * 100;\n        pos[i*3] = Math.cos(angle) * r;\n        pos[i*3+1] = (Math.random() - 0.5) * 20;\n        pos[i*3+2] = Math.sin(angle) * r;\n        col[i*3] = 1; col[i*3+1] = Math.random() * 0.5; col[i*3+2] = 0;\n    }\n    geo.setAttribute('position', new THREE.BufferAttribute(pos, 3));\n    geo.setAttribute('color', new THREE.BufferAttribute(col, 3));\n    var pts = new THREE.Points(geo, new THREE.PointsMaterial({\n        size: 2, vertexColors: true, transparent: true, opacity: 0.8, blending: THREE.AdditiveBlending\n    }));\n    bhGroup.add(pts);\n\n    bhGroup.position.copy(position);\n    blackHoles.push({ group: bhGroup, disk: disk, particles: pts, nodeName: nodeName, pullRadius: 150, pullForce: 0.5 });\n    scene.add(bhGroup);\n    return bhGroup;\n}\n\n// ─────────────────────────────────────────────────────────────────────────\n// 💥 SUPERNOVAS\n// ─────────────────────────────────────────────────────────────────────────\n\nfunction createSupernova(position, nodeName, intensity) {\n    var snGroup = new THREE.Group();\n\n    // Ядро\n    var coreMat = new THREE.MeshBasicMaterial({ color: 0xffffff, transparent: true, opacity: 1 });\n    var core = new THREE.Mesh(new THREE.SphereGeometry(10, 16, 16), coreMat);\n    snGroup.add(core);\n\n    // Взрывная волна\n    var waveMat = new THREE.MeshBasicMaterial({ color: 0x00ffff, transparent: true, opacity: 0.5, side: THREE.DoubleSide });\n    var wave = new THREE.Mesh(new THREE.SphereGeometry(1, 32, 32), waveMat);\n    snGroup.add(wave);\n\n    // Частицы взрыва\n    var pCount = 2000;\n    var geo = new THREE.BufferGeometry();\n    var pos = new Float32Array(pCount * 3);\n    var vel = new Float32Array(pCount * 3);\n    var col = new Float32Array(pCount * 3);\n    for (var i = 0; i < pCount; i++) {\n        var theta = Math.random() * Math.PI * 2;\n        var phi = Math.random() * Math.PI;\n        var speed = 2 + Math.random() * 5;\n        pos[i*3] = 0; pos[i*3+1] = 0; pos[i*3+2] = 0;\n        vel[i*3] = Math.sin(phi) * Math.cos(theta) * speed;\n        vel[i*3+1] = Math.cos(phi) * speed;\n        vel[i*3+2] = Math.sin(phi) * Math.sin(theta) * speed;\n        var cm = Math.random();\n        col[i*3] = 1; col[i*3+1] = 1 - cm*0.5; col[i*3+2] = 1 - cm;\n    }\n    geo.setAttribute('position', new THREE.BufferAttribute(pos, 3));\n    geo.setAttribute('color', new THREE.BufferAttribute(col, 3));\n    var pts = new THREE.Points(geo, new THREE.PointsMaterial({\n        size: 3, vertexColors: true, transparent: true, opacity: 1, blending: THREE.AdditiveBlending\n    }));\n    snGroup.add(pts);\n\n    snGroup.position.copy(position);\n    supernovas.push({\n        group: snGroup, core: core, wave: wave, particles: pts, velocities: vel,\n        nodeName: nodeName, createdAt: Date.now(), phase: 'explosion', intensity: intensity || 1\n    });\n    scene.add(snGroup);\n\n    // Вспышка\n    var flash = new THREE.AmbientLight(0xffffff, 5);\n    scene.add(flash);\n    var fi = 5;\n    var fiv = setInterval(function() { fi -= 0.2; flash.intensity = fi; if (fi <= 0) { scene.remove(flash); clearInterval(fiv); } }, 50);\n\n    return snGroup;\n}\n\n// ─────────────────────────────────────────────────────────────────────────\n// 🌀 WORMHOLES\n// ─────────────────────────────────────────────────────────────────────────\n\nfunction createWormholePortal(color) {\n    var pg = new THREE.Group();\n    var ring = new THREE.Mesh(\n        new THREE.TorusGeometry(15, 2, 16, 100),\n        new THREE.MeshBasicMaterial({ color: color, transparent: true, opacity: 0.8 })\n    );\n    pg.add(ring);\n    var vortex = new THREE.Mesh(\n        new THREE.ConeGeometry(10, 20, 32, 1, true),\n        new THREE.MeshBasicMaterial({ color: color, transparent: true, opacity: 0.3, side: THREE.DoubleSide })\n    );\n    pg.add(vortex);\n    return { group: pg, ring: ring, vortex: vortex };\n}\n\nfunction createWormhole(startPos, endPos, connectionName) {\n    var whGroup = new THREE.Group();\n\n    var portal1 = createWormholePortal(0x00ffff);\n    portal1.group.position.copy(startPos);\n    whGroup.add(portal1.group);\n\n    var portal2 = createWormholePortal(0xff00ff);\n    portal2.group.position.copy(endPos);\n    whGroup.add(portal2.group);\n\n    // Кривая туннеля\n    var midPoint = new THREE.Vector3().addVectors(startPos, endPos).multiplyScalar(0.5);\n    midPoint.y += 50;\n    var curve = new THREE.QuadraticBezierCurve3(startPos.clone(), midPoint, endPos.clone());\n\n    // Частицы туннеля\n    var tCount = 200;\n    var geo = new THREE.BufferGeometry();\n    var pos = new Float32Array(tCount * 3);\n    var col = new Float32Array(tCount * 3);\n    for (var i = 0; i < tCount; i++) {\n        var t = i / tCount;\n        var pt = curve.getPoint(t);\n        pos[i*3] = pt.x + (Math.random()-0.5)*10;\n        pos[i*3+1] = pt.y + (Math.random()-0.5)*10;\n        pos[i*3+2] = pt.z + (Math.random()-0.5)*10;\n        col[i*3] = t; col[i*3+1] = 1 - t*0.5; col[i*3+2] = 1 - t;\n    }\n    geo.setAttribute('position', new THREE.BufferAttribute(pos, 3));\n    geo.setAttribute('color', new THREE.BufferAttribute(col, 3));\n    var tunnel = new THREE.Points(geo, new THREE.PointsMaterial({\n        size: 2, vertexColors: true, transparent: true, opacity: 0.6, blending: THREE.AdditiveBlending\n    }));\n    whGroup.add(tunnel);\n\n    wormholes.push({\n        group: whGroup, portal1: portal1, portal2: portal2,\n        curve: curve, tunnelParticles: tunnel, connectionName: connectionName\n    });\n    scene.add(whGroup);\n    return whGroup;\n}\n\n// ─────────────────────────────────────────────────────────────────────────\n// SHAPE POSITIONS\n// ─────────────────────────────────────────────────────────────────────────\n\nfunction getShapePositions(shape, count) {\n    const positions = [];\n    \n    switch (shape) {\n        case 'galaxy':\n            for (let i = 0; i < count; i++) {\n                const angle = (i / count) * Math.PI * 20;\n                const radius = i * 2;\n                const height = Math.sin(i * 0.1) * 50;\n                positions.push(new THREE.Vector3(\n                    Math.cos(angle) * radius,\n                    height + (Math.random() - 0.5) * 20,\n                    Math.sin(angle) * radius\n                ));\n            }\n            break;\n            \n        case 'sphere':\n            for (let i = 0; i < count; i++) {\n                const phi = Math.acos(-1 + (2 * i) / count);\n                const theta = Math.sqrt(count * Math.PI) * phi;\n                const radius = 200;\n                positions.push(new THREE.Vector3(\n                    Math.cos(theta) * Math.sin(phi) * radius,\n                    Math.sin(theta) * Math.sin(phi) * radius,\n                    Math.cos(phi) * radius\n                ));\n            }\n            break;\n            \n        case 'cube':\n            const side = Math.ceil(Math.cbrt(count));\n            const spacing = 50;\n            let idx = 0;\n            for (let x = 0; x < side && idx < count; x++) {\n                for (let y = 0; y < side && idx < count; y++) {\n                    for (let z = 0; z < side && idx < count; z++) {\n                        positions.push(new THREE.Vector3(\n                            (x - side/2) * spacing,\n                            (y - side/2) * spacing,\n                            (z - side/2) * spacing\n                        ));\n                        idx++;\n                    }\n                }\n            }\n            break;\n            \n        case 'helix':\n            for (let i = 0; i < count; i++) {\n                const t = i / count * Math.PI * 10;\n                const radius = 100;\n                positions.push(new THREE.Vector3(\n                    Math.cos(t) * radius,\n                    (i / count - 0.5) * 500,\n                    Math.sin(t) * radius\n                ));\n                // Second strand\n                if (i % 2 === 0) {\n                    positions[i].x = Math.cos(t + Math.PI) * radius;\n                    positions[i].z = Math.sin(t + Math.PI) * radius;\n                }\n            }\n            break;\n            \n        case 'torus':\n            for (let i = 0; i < count; i++) {\n                const u = (i / count) * Math.PI * 2;\n                const v = ((i * 7) % count) / count * Math.PI * 2;\n                const R = 200; // Major radius\n                const r = 100; // Minor radius\n                positions.push(new THREE.Vector3(\n                    (R + r * Math.cos(v)) * Math.cos(u),\n                    (R + r * Math.cos(v)) * Math.sin(u),\n                    r * Math.sin(v)\n                ));\n            }\n            break;\n            \n        case 'wave':\n            const rows = Math.ceil(Math.sqrt(count));\n            const cols = Math.ceil(count / rows);\n            let waveIdx = 0;\n            for (let row = 0; row < rows && waveIdx < count; row++) {\n                for (let col = 0; col < cols && waveIdx < count; col++) {\n                    const x = (col - cols/2) * 30;\n                    const z = (row - rows/2) * 30;\n                    const y = Math.sin(x * 0.05) * 50 + Math.cos(z * 0.05) * 50;\n                    positions.push(new THREE.Vector3(x, y, z));\n                    waveIdx++;\n                }\n            }\n            break;\n            \n        default:\n            // Random positions\n            for (let i = 0; i < count; i++) {\n                positions.push(new THREE.Vector3(\n                    (Math.random() - 0.5) * 500,\n                    (Math.random() - 0.5) * 500,\n                    (Math.random() - 0.5) * 500\n                ));\n            }\n    }\n    \n    return positions;\n}\n\n// ─────────────────────────────────────────────────────────────────────────\n// ANIMATION\n// ─────────────────────────────────────────────────────────────────────────\n\nfunction animate() {\n    requestAnimationFrame(animate);\n    \n    const time = Date.now() * 0.001 * animationSpeed;\n    \n    // Auto-rotate\n    if (autoRotate && scene.children.length > 0) {\n        scene.rotation.y = time * 0.1;\n    }\n\n    // Node animations\n    nodes.forEach(node => {\n        // Floating animation\n        node.mesh.position.y = node.originalPosition.y + Math.sin(time + node.mesh.position.x * 0.01) * 5;\n        \n        // Rotation\n        node.mesh.rotation.x = time * 0.5;\n        node.mesh.rotation.y = time * 0.3;\n        \n        // Gravity simulation\n        if (gravityEnabled) {\n            const center = new THREE.Vector3(0, 0, 0);\n            const force = center.clone().sub(node.mesh.position).normalize().multiplyScalar(0.01);\n            node.mesh.userData.velocity.add(force);\n            node.mesh.userData.velocity.multiplyScalar(0.99); // Damping\n            node.mesh.position.add(node.mesh.userData.velocity);\n        }\n        \n        // Glow pulsing\n        if (node.glow) {\n            node.glow.material.opacity = 0.2 + Math.sin(time * 2 + node.pulseOffset) * 0.1 + ((node.data.heat || 0) * 0.12);\n        }\n        if (node.halo) {\n            node.halo.rotation.z = time * 0.8;\n            node.halo.material.opacity = 0.45 + Math.sin(time * 3 + node.pulseOffset) * 0.08;\n        }\n        if (node.outline) {\n            node.outline.rotation.y = time * 0.4;\n        }\n        if (node.errorAura) {\n            node.errorAura.material.opacity = 0.12 + Math.sin(time * 6 + node.pulseOffset) * 0.08;\n        }\n    });\n    \n    // Update edges\n    edges.forEach(edge => {\n        const positions = edge.line.geometry.attributes.position.array;\n        positions[0] = edge.source.mesh.position.x;\n        positions[1] = edge.source.mesh.position.y;\n        positions[2] = edge.source.mesh.position.z;\n        positions[3] = edge.target.mesh.position.x;\n        positions[4] = edge.target.mesh.position.y;\n        positions[5] = edge.target.mesh.position.z;\n        edge.line.geometry.attributes.position.needsUpdate = true;\n    });\n    \n    // Particles animation\n    if (particles && particlesEnabled) {\n        particles.rotation.y = time * 0.05;\n        particles.rotation.x = time * 0.03;\n    }\n\n    // ═══ COSMIC EFFECTS ANIMATION ═══\n\n    // Black holes\n    blackHoles.forEach(function(bh) {\n        bh.disk.rotation.z = time * 2;\n        var bpos = bh.particles.geometry.attributes.position.array;\n        for (var i = 0; i < bpos.length; i += 3) {\n            var bx = bpos[i], bz = bpos[i+2];\n            var br = Math.sqrt(bx*bx + bz*bz);\n            var ba = Math.atan2(bz, bx) + 0.05;\n            var nr = br * 0.98;\n            bpos[i] = Math.cos(ba) * nr;\n            bpos[i+2] = Math.sin(ba) * nr;\n            if (nr < 5) {\n                var ra = Math.random() * Math.PI * 2;\n                var rr = 50 + Math.random() * 100;\n                bpos[i] = Math.cos(ra) * rr;\n                bpos[i+2] = Math.sin(ra) * rr;\n            }\n        }\n        bh.particles.geometry.attributes.position.needsUpdate = true;\n    });\n\n    // Supernovas\n    for (var si = supernovas.length - 1; si >= 0; si--) {\n        var sn = supernovas[si];\n        var age = (Date.now() - sn.createdAt) / 1000;\n        if (sn.phase === 'explosion') {\n            sn.wave.scale.setScalar(1 + age * 50);\n            sn.wave.material.opacity = Math.max(0, 0.5 - age * 0.1);\n            var sp = sn.particles.geometry.attributes.position.array;\n            var sv = sn.velocities;\n            for (var j = 0; j < sp.length; j += 3) {\n                sp[j] += sv[j] * 0.5; sp[j+1] += sv[j+1] * 0.5; sp[j+2] += sv[j+2] * 0.5;\n                sv[j] *= 0.98; sv[j+1] *= 0.98; sv[j+2] *= 0.98;\n            }\n            sn.particles.geometry.attributes.position.needsUpdate = true;\n            sn.core.scale.setScalar(Math.max(0.1, 1 - age * 0.1));\n            if (age > 5) sn.phase = 'fading';\n        } else {\n            sn.particles.material.opacity *= 0.95;\n            sn.core.material.opacity *= 0.95;\n            if (sn.particles.material.opacity < 0.01) {\n                scene.remove(sn.group);\n                supernovas.splice(si, 1);\n            }\n        }\n    }\n\n    // Wormholes\n    wormholes.forEach(function(wh) {\n        wh.portal1.ring.rotation.z = time * 3;\n        wh.portal1.vortex.rotation.y = -time * 5;\n        wh.portal2.ring.rotation.z = -time * 3;\n        wh.portal2.vortex.rotation.y = time * 5;\n        var pf = 1 + Math.sin(time * 4) * 0.2;\n        wh.portal1.ring.scale.setScalar(pf);\n        wh.portal2.ring.scale.setScalar(pf);\n        var wp = wh.tunnelParticles.geometry.attributes.position.array;\n        var wc = wh.tunnelParticles.geometry.attributes.color.array;\n        var wl = wp.length / 3;\n        for (var k = 0; k < wl; k++) {\n            var wt = ((k / wl) + time * 0.2) % 1;\n            if (wt < 0) wt += 1;\n            var pt = wh.curve.getPoint(wt);\n            var sa = wt * Math.PI * 10 + time * 2;\n            var sr = 5 + Math.sin(wt * Math.PI) * 5;\n            wp[k*3] = pt.x + Math.cos(sa) * sr;\n            wp[k*3+1] = pt.y + Math.sin(sa) * sr;\n            wp[k*3+2] = pt.z + Math.sin(sa + Math.PI/2) * sr;\n            wc[k*3] = Math.sin(wt * Math.PI);\n            wc[k*3+1] = Math.cos(wt * Math.PI * 0.5);\n            wc[k*3+2] = 1 - wt;\n        }\n        wh.tunnelParticles.geometry.attributes.position.needsUpdate = true;\n        wh.tunnelParticles.geometry.attributes.color.needsUpdate = true;\n    });\n\n    // Render\n    renderer.render(scene, camera);\n    \n    // Update FPS\n    updateFPS();\n}\n\n// ─────────────────────────────────────────────────────────────────────────\n// CONTROLS\n// ─────────────────────────────────────────────────────────────────────────\n\nfunction setShape(shape) {\n    currentShape = shape;\n    \n    // Update button states\n    document.querySelectorAll('.shape-btn').forEach(btn => {\n        btn.classList.toggle('active', btn.dataset.shape === shape);\n    });\n    \n    // Rearrange nodes\n    const positions = getShapePositions(shape, nodes.size);\n    let i = 0;\n    nodes.forEach(node => {\n        const newPos = positions[i++];\n        \n        // Animate to new position\n        const startPos = node.mesh.position.clone();\n        const duration = 1000;\n        const startTime = Date.now();\n        \n        const animatePosition = () => {\n            const elapsed = Date.now() - startTime;\n            const progress = Math.min(elapsed / duration, 1);\n            const eased = easeInOutCubic(progress);\n            \n            node.mesh.position.lerpVectors(startPos, newPos, eased);\n            node.originalPosition = newPos.clone();\n            \n            if (progress < 1) {\n                requestAnimationFrame(animatePosition);\n            }\n        };\n        animatePosition();\n    });\n}\n\nfunction setMode(mode) {\n    currentMode = mode === 'nexus' ? 'nexus' : 'classic';\n    resetCamera();\n\n    document.querySelectorAll('.mode-btn').forEach(btn => {\n        btn.classList.toggle('active', btn.dataset.mode === currentMode);\n    });\n\n    if (currentMode === 'nexus') {\n        createVisualization(nexusData);\n    } else {\n        createVisualization(classicData);\n    }\n\n    closeInfo();\n    vscode.postMessage({ type: 'switchMode', mode: currentMode });\n}\n\nfunction setColorScheme(scheme) {\n    currentColorScheme = scheme;\n    const colors = COLOR_SCHEMES[scheme];\n    \n    // Update background\n    scene.background = new THREE.Color(colors.bg);\n    scene.fog.color = new THREE.Color(colors.bg);\n    \n    // Update nodes\n    nodes.forEach(node => {\n        const color = resolveNodeColor(node.data);\n        node.mesh.material.color = new THREE.Color(color);\n        node.mesh.material.emissive = new THREE.Color(color);\n        if (node.glow) {\n            node.glow.material.color = new THREE.Color(color);\n        }\n        if (node.halo) {\n            node.halo.material.color = new THREE.Color(0xffd166);\n        }\n        if (node.outline) {\n            node.outline.material.color = new THREE.Color(\n                node.data.isPending ? 0x3a86ff : color\n            );\n        }\n        if (node.errorAura) {\n            node.errorAura.material.color = new THREE.Color(0xff335e);\n        }\n    });\n    \n    // Update edges\n    edges.forEach(edge => {\n        const edgeColor = colors.edge[edge.data.type] || 0xffffff;\n        edge.line.material.color = new THREE.Color(edgeColor);\n    });\n    \n    // Update particles\n    if (particles) {\n        const particleColor = new THREE.Color(colors.particle);\n        const colors = particles.geometry.attributes.color.array;\n        for (let i = 0; i < colors.length; i += 3) {\n            colors[i] = particleColor.r;\n            colors[i + 1] = particleColor.g;\n            colors[i + 2] = particleColor.b;\n        }\n        particles.geometry.attributes.color.needsUpdate = true;\n    }\n}\n\nfunction toggleGravity() {\n    gravityEnabled = document.getElementById('gravity').checked;\n    \n    // Reset velocities if gravity disabled\n    if (!gravityEnabled) {\n        nodes.forEach(node => {\n            node.mesh.userData.velocity = new THREE.Vector3(0, 0, 0);\n        });\n    }\n}\n\nfunction toggleRotation() {\n    autoRotate = document.getElementById('rotation').checked;\n}\n\nfunction toggleParticles() {\n    particlesEnabled = document.getElementById('particles').checked;\n    if (particles) {\n        particles.visible = particlesEnabled;\n    }\n}\n\nfunction setSpeed(value) {\n    animationSpeed = value / 100;\n    document.getElementById('speedValue').textContent = value + '%';\n}\n\nfunction setZoom(value) {\n    const targetZ = 500 * (2 - value / 100);\n    camera.position.z = targetZ;\n}\n\nfunction toggleEdgeType(type) {\n    edgeFilters[type] = !edgeFilters[type];\n    \n    edges.forEach(edge => {\n        edge.line.visible = edgeFilters[edge.data.type];\n    });\n    \n    updateStats();\n}\n\nfunction resetCamera() {\n    camera.position.set(0, 0, 500);\n    scene.rotation.set(0, 0, 0);\n}\n\nfunction explodeView() {\n    nodes.forEach(node => {\n        const direction = node.mesh.position.clone().normalize();\n        if (direction.length() === 0) {\n            direction.set(Math.random() - 0.5, Math.random() - 0.5, Math.random() - 0.5).normalize();\n        }\n        node.mesh.userData.velocity = direction.multiplyScalar(5);\n    });\n}\n\nfunction implodeView() {\n    nodes.forEach(node => {\n        const direction = node.originalPosition.clone().sub(node.mesh.position).normalize();\n        node.mesh.userData.velocity = direction.multiplyScalar(5);\n    });\n}\n\nfunction refreshData() {\n    vscode.postMessage({ type: 'refresh', mode: currentMode });\n}\n\n// ─────────────────────────────────────────────────────────────────────────\n// INTERACTION\n// ─────────────────────────────────────────────────────────────────────────\n\nfunction onNodeClick(event) {\n    const intersects = getNodeIntersections(event);\n    \n    if (intersects.length > 0) {\n        const selectedNode = intersects[0].object;\n        const data = selectedNode.userData;\n        selectedNodeId = getNodeId(data);\n        selectedNodeData = data;\n        \n        highlightSelection(selectedNodeId);\n        \n        // Show info\n        showNodeInfo(data);\n        \n        if (currentMode === 'nexus') {\n            vscode.postMessage({\n                type: 'requestNexusScan',\n                path: getNodePath(data),\n                priority: 'focus'\n            });\n        } else {\n            vscode.postMessage({\n                type: 'openFile',\n                path: getNodePath(data)\n            });\n        }\n    }\n}\n\nfunction onNodeHover(event) {\n    if (currentMode !== 'nexus') {\n        renderer.domElement.style.cursor = 'default';\n        return;\n    }\n\n    const intersects = getNodeIntersections(event);\n    if (intersects.length === 0) {\n        renderer.domElement.style.cursor = 'default';\n        hoveredNodeId = null;\n        if (hoverScanTimer) {\n            clearTimeout(hoverScanTimer);\n            hoverScanTimer = null;\n        }\n        return;\n    }\n\n    renderer.domElement.style.cursor = 'pointer';\n    const hovered = intersects[0].object.userData;\n    const nextNodeId = getNodeId(hovered);\n    if (hoveredNodeId === nextNodeId) {\n        return;\n    }\n\n    hoveredNodeId = nextNodeId;\n    if (hoverScanTimer) {\n        clearTimeout(hoverScanTimer);\n    }\n    hoverScanTimer = setTimeout(() => {\n        if (hoveredNodeId !== nextNodeId) {\n            return;\n        }\n        vscode.postMessage({\n            type: 'requestNexusScan',\n            path: getNodePath(hovered),\n            priority: 'visible'\n        });\n    }, 220);\n}\n\nfunction getNodeIntersections(event) {\n    const raycaster = new THREE.Raycaster();\n    const mouse = new THREE.Vector2();\n\n    mouse.x = (event.clientX / window.innerWidth) * 2 - 1;\n    mouse.y = -(event.clientY / window.innerHeight) * 2 + 1;\n\n    raycaster.setFromCamera(mouse, camera);\n\n    const meshes = Array.from(nodes.values()).map(n => n.mesh);\n    return raycaster.intersectObjects(meshes);\n}\n\nfunction showNodeInfo(data) {\n    const panel = document.getElementById('infoPanel');\n    const title = document.getElementById('infoTitle');\n    const content = document.getElementById('infoContent');\n    \n    title.textContent = data.label || 'Unknown';\n\n    if (currentMode === 'nexus') {\n        const scan = nexusScanResults.get(getNodeId(data));\n        const indexSummary = scan?.indexSummary;\n        const badges = [];\n\n        if (data.isPending) badges.push('<span class=\"info-badge pending\">Pending</span>');\n        if (data.isStarred) badges.push('<span class=\"info-badge starred\">Starred</span>');\n        if ((data.heat || 0) > 0) badges.push('<span class=\"info-badge active\">Heat ' + Math.round((data.heat || 0) * 100) + '%</span>');\n        if (scan?.issues?.some(issue => issue.severity === 'error')) {\n            badges.push('<span class=\"info-badge error\">Errors</span>');\n        } else if (scan?.issues?.some(issue => issue.severity === 'warning')) {\n            badges.push('<span class=\"info-badge warning\">Warnings</span>');\n        }\n\n        const symbols = (scan?.symbols || []).slice(0, 8).map(symbol => `\n            <div class=\"info-list-item\">\n                <div class=\"info-list-meta\">\n                    <span>${escapeHtml(symbol.kind)}</span>\n                    <span>Lines ${symbol.startLine + 1}-${symbol.endLine + 1}</span>\n                </div>\n                <div>${escapeHtml(symbol.fullName || symbol.name)}</div>\n            </div>\n        `).join('');\n\n        const issues = ((scan?.indexedIssues && scan.indexedIssues.length > 0)\n            ? scan.indexedIssues\n            : (scan?.issues || [])).slice(0, 8).map(issue => `\n            <div class=\"info-list-item clickable\" onclick=\"openCurrentInfoFile(${issue.line}, ${issue.column || 1})\">\n                <div class=\"info-list-meta\">\n                    <span>${escapeHtml(issue.severity)}</span>\n                    <span>${escapeHtml(issue.source || 'local')}</span>\n                    ${typeof issue.hitCount === 'number' ? '<span>Seen ' + issue.hitCount + 'x</span>' : ''}\n                </div>\n                ${typeof issue.firstSeen === 'number' ? '<div class=\"info-list-meta\"><span>First seen ' + escapeHtml(formatScanTimestamp(issue.firstSeen)) + '</span></div>' : ''}\n                <div>[${issue.line}:${issue.column || 1}] ${escapeHtml(issue.message)}</div>\n            </div>\n        `).join('');\n\n        content.innerHTML = `\n            <div class=\"info-badges\">${badges.join('')}</div>\n            <div class=\"info-item\">\n                <span class=\"info-label\">Path</span>\n                <span class=\"info-value path\">${escapeHtml(getNodePath(data))}</span>\n            </div>\n            <div class=\"info-item\">\n                <span class=\"info-label\">Graph Weight</span>\n                <span class=\"info-value\">${getNodeSize(data)}</span>\n            </div>\n            <div class=\"info-item\">\n                <span class=\"info-label\">Connections</span>\n                <span class=\"info-value\">${countConnections(getNodeId(data))}</span>\n            </div>\n            <div class=\"info-item\">\n                <span class=\"info-label\">Group Connections</span>\n                <span class=\"info-value\">${countConnectionsByType(getNodeId(data), 'group')}</span>\n            </div>\n            <div class=\"info-item\">\n                <span class=\"info-label\">Temporal Connections</span>\n                <span class=\"info-value\">${countConnectionsByType(getNodeId(data), 'temporal')}</span>\n            </div>\n            <div class=\"info-item\">\n                <span class=\"info-label\">Last LocalHub Change</span>\n                <span class=\"info-value\">${escapeHtml(formatTimestamp(data.lastChange || 0))}</span>\n            </div>\n            <div class=\"info-item\">\n                <span class=\"info-label\">Scan State</span>\n                <span class=\"info-value\">${escapeHtml(scan?.state || data.scanState || 'idle')}</span>\n            </div>\n            <div class=\"info-item\">\n                <span class=\"info-label\">Last Scan</span>\n                <span class=\"info-value\">${escapeHtml(formatScanTimestamp(scan?.scannedAt || 0))}</span>\n            </div>\n            <div class=\"info-item\">\n                <span class=\"info-label\">Issue Memory</span>\n                <span class=\"info-value\">${escapeHtml(indexSummary ? (indexSummary.activeCount + ' active / ' + indexSummary.resolvedCount + ' resolved') : 'live only')}</span>\n            </div>\n            <div class=\"info-item\">\n                <span class=\"info-label\">Indexed At</span>\n                <span class=\"info-value\">${escapeHtml(formatScanTimestamp(indexSummary?.lastIndexedAt || 0))}</span>\n            </div>\n            <div class=\"info-actions\">\n                <button class=\"info-action-btn\" onclick=\"openCurrentInfoFile()\">Open File</button>\n                <button class=\"info-action-btn\" onclick=\"requestCurrentNodeScan()\">Rescan</button>\n            </div>\n            <div class=\"info-section-title\">Architecture</div>\n            <div class=\"info-list\">${symbols || '<div class=\"info-list-item\">No symbols yet</div>'}</div>\n            <div class=\"info-section-title\">Issues</div>\n            <div class=\"info-list\">${issues || '<div class=\"info-list-item\">No issues yet</div>'}</div>\n        `;\n    } else {\n        content.innerHTML = `\n            <div class=\"info-item\">\n                <span class=\"info-label\">Path</span>\n                <span class=\"info-value path\">${escapeHtml(getNodePath(data))}</span>\n            </div>\n            <div class=\"info-item\">\n                <span class=\"info-label\">Size</span>\n                <span class=\"info-value\">${data.size || 0}</span>\n            </div>\n            <div class=\"info-item\">\n                <span class=\"info-label\">Connections</span>\n                <span class=\"info-value\">${countConnections(getNodeId(data))}</span>\n            </div>\n        `;\n    }\n    \n    panel.classList.add('show');\n}\n\nfunction closeInfo() {\n    document.getElementById('infoPanel').classList.remove('show');\n    selectedNodeId = null;\n    selectedNodeData = null;\n    clearSelectionHighlight();\n}\n\nfunction openCurrentInfoFile(line, column) {\n    if (!selectedNodeData) return;\n    vscode.postMessage({\n        type: 'openFile',\n        path: getNodePath(selectedNodeData),\n        line: typeof line === 'number' ? line : undefined,\n        column: typeof column === 'number' ? column : undefined\n    });\n}\n\nfunction requestCurrentNodeScan() {\n    if (!selectedNodeData) return;\n    vscode.postMessage({\n        type: 'requestNexusScan',\n        path: getNodePath(selectedNodeData),\n        priority: 'focus',\n        force: true\n    });\n}\n\nfunction applyScanResultToNode(scan) {\n    const node = nodes.get(scan.path);\n    if (!node) {\n        return;\n    }\n\n    if (scan.issues && scan.issues.some(issue => issue.severity === 'error')) {\n        if (!node.errorAura) {\n            const geometry = new THREE.SphereGeometry(node.mesh.geometry.parameters.radius + 3, 10, 10);\n            const material = new THREE.MeshBasicMaterial({\n                color: 0xff335e,\n                transparent: true,\n                opacity: 0.18\n            });\n            node.errorAura = new THREE.Mesh(geometry, material);\n            node.mesh.add(node.errorAura);\n        }\n    } else if (node.errorAura) {\n        node.mesh.remove(node.errorAura);\n        node.errorAura.geometry.dispose();\n        node.errorAura.material.dispose();\n        node.errorAura = null;\n    }\n}\n\nfunction highlightSelection(nodeId) {\n    const connectedIds = new Set([nodeId]);\n\n    edges.forEach(edge => {\n        const isConnected = edge.data.source === nodeId || edge.data.target === nodeId;\n        if (isConnected) {\n            connectedIds.add(edge.data.source);\n            connectedIds.add(edge.data.target);\n        }\n        edge.line.material.opacity = isConnected && edgeFilters[edge.data.type] ? 0.95 : 0.08;\n    });\n\n    nodes.forEach((node, id) => {\n        if (id === nodeId) {\n            node.mesh.material.opacity = 1;\n            node.mesh.scale.setScalar(1.18);\n        } else if (connectedIds.has(id)) {\n            node.mesh.material.opacity = 0.95;\n            node.mesh.scale.setScalar(1.04);\n        } else {\n            node.mesh.material.opacity = 0.18;\n            node.mesh.scale.setScalar(0.92);\n        }\n    });\n}\n\nfunction clearSelectionHighlight() {\n    nodes.forEach(node => {\n        node.mesh.material.opacity = 0.9;\n        node.mesh.scale.setScalar(1);\n    });\n    edges.forEach(edge => {\n        edge.line.material.opacity = edgeFilters[edge.data.type]\n            ? 0.3 + edge.data.weight * 0.1\n            : 0;\n    });\n}\n\n\nfunction countConnections(nodeId) {\n    return edges.filter(e => \n        (e.data.source === nodeId || e.data.target === nodeId) && \n        edgeFilters[e.data.type]\n    ).length;\n}\n\nfunction countConnectionsByType(nodeId, edgeType) {\n    return edges.filter(e =>\n        (e.data.source === nodeId || e.data.target === nodeId) &&\n        e.data.type === edgeType\n    ).length;\n}\n\n// ─────────────────────────────────────────────────────────────────────────\n// HELPERS\n// ─────────────────────────────────────────────────────────────────────────\n\nfunction clearVisualization() {\n    // Remove nodes\n    nodes.forEach(node => {\n        scene.remove(node.mesh);\n    });\n    nodes.clear();\n    \n    // Remove edges\n    edges.forEach(edge => {\n        scene.remove(edge.line);\n    });\n    edges = [];\n}\n\nfunction updateStats() {\n    const visibleEdges = edges.filter(e => edgeFilters[e.data.type]).length;\n    document.getElementById('nodeCount').textContent = nodes.size;\n    document.getElementById('edgeCount').textContent = visibleEdges;\n}\n\nlet lastTime = 0;\nlet frameCount = 0;\nfunction updateFPS() {\n    frameCount++;\n    const currentTime = Date.now();\n    if (currentTime >= lastTime + 1000) {\n        document.getElementById('fps').textContent = frameCount;\n        frameCount = 0;\n        lastTime = currentTime;\n    }\n}\n\nfunction easeInOutCubic(t) {\n    return t < 0.5 \n        ? 4 * t * t * t \n        : 1 - Math.pow(-2 * t + 2, 3) / 2;\n}\n\nfunction onWindowResize() {\n    camera.aspect = window.innerWidth / window.innerHeight;\n    camera.updateProjectionMatrix();\n    renderer.setSize(window.innerWidth, window.innerHeight);\n}\n\n// ─────────────────────────────────────────────────────────────────────────\n// MESSAGE HANDLER\n// ─────────────────────────────────────────────────────────────────────────\n\nwindow.addEventListener('message', event => {\n    const message = event.data;\n    switch (message.type) {\n        case 'updateData':\n            currentData = message.data;\n            createVisualization(message.data);\n            break;\n        case 'updateNexusData':\n            nexusData = message.data;\n            if (currentMode === 'nexus') {\n                createVisualization(nexusData);\n            }\n            break;\n        case 'nexusScanUpdate':\n            if (message.data?.path) {\n                nexusScanResults.set(message.data.path, message.data);\n                applyScanResultToNode(message.data);\n                if (selectedNodeData && getNodePath(selectedNodeData) === message.data.path) {\n                    showNodeInfo(selectedNodeData);\n                }\n            }\n            break;\n    }\n});\n\n// ─────────────────────────────────────────────────────────────────────────\n// 🌌 COSMIC TEST FUNCTIONS\n// ─────────────────────────────────────────────────────────────────────────\n\nfunction testBlackHole() {\n    var arr = Array.from(nodes.values());\n    if (arr.length > 0) {\n        var n = arr[Math.floor(Math.random() * arr.length)];\n        createBlackHole(n.mesh.position.clone(), n.data.label || 'Unknown');\n    }\n}\n\nfunction testSupernova() {\n    var arr = Array.from(nodes.values());\n    if (arr.length > 0) {\n        var n = arr[Math.floor(Math.random() * arr.length)];\n        createSupernova(n.mesh.position.clone(), n.data.label || 'Unknown', 2);\n    }\n}\n\nfunction testWormhole() {\n    var arr = Array.from(nodes.values());\n    if (arr.length >= 2) {\n        var n1 = arr[Math.floor(Math.random() * arr.length)];\n        var n2 = arr[Math.floor(Math.random() * arr.length)];\n        while (n2 === n1 && arr.length > 1) {\n            n2 = arr[Math.floor(Math.random() * arr.length)];\n        }\n        createWormhole(\n            n1.mesh.position.clone(),\n            n2.mesh.position.clone(),\n            (n1.data.label || '?') + ' <-> ' + (n2.data.label || '?')\n        );\n    }\n}\n\n// ─────────────────────────────────────────────────────────────────────────\n// START\n// ─────────────────────────────────────────────────────────────────────────\n\ninit();\n"}}function register3DGrid(n,e){const t=new nexus3d_error_index_1.Nexus3DErrorIndex(n.workspaceState,e.projectRoot),o=new Grid3DWebviewProvider(n.extensionUri,e,t);n.subscriptions.push(vscode.window.registerWebviewViewProvider(Grid3DWebviewProvider.viewType,o)),n.subscriptions.push(vscode.commands.registerCommand("localhub.open3DGrid",()=>{o.openFullscreen()})),n.subscriptions.push(t,o,vscode.workspace.onDidOpenTextDocument(n=>{"file"===n.uri.scheme&&o.handleDocumentActivity(n.uri.fsPath,"visible")}),vscode.workspace.onDidChangeTextDocument(n=>{"file"===n.document.uri.scheme&&o.handleDocumentActivity(n.document.uri.fsPath,"visible")}),vscode.workspace.onDidSaveTextDocument(n=>{"file"===n.uri.scheme&&(o.handleDocumentActivity(n.uri.fsPath,"visible"),o.scheduleNexusWorldRefresh(1e3))}),vscode.window.onDidChangeVisibleTextEditors(n=>{for(const e of n)"file"===e.document.uri.scheme&&o.handleDocumentActivity(e.document.uri.fsPath,"visible");o.scheduleNexusWorldRefresh(600)})),console.log("[LocalHub] 3D Grid visualization registered")}exports.Grid3DWebviewProvider=Grid3DWebviewProvider;