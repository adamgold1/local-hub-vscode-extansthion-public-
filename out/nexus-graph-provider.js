"use strict";var __createBinding=this&&this.__createBinding||(Object.create?function(n,e,t,a){void 0===a&&(a=t);var o=Object.getOwnPropertyDescriptor(e,t);o&&!("get"in o?!e.__esModule:o.writable||o.configurable)||(o={enumerable:!0,get:function(){return e[t]}}),Object.defineProperty(n,a,o)}:function(n,e,t,a){void 0===a&&(a=t),n[a]=e[t]}),__setModuleDefault=this&&this.__setModuleDefault||(Object.create?function(n,e){Object.defineProperty(n,"default",{enumerable:!0,value:e})}:function(n,e){n.default=e}),__importStar=this&&this.__importStar||function(){var n=function(e){return n=Object.getOwnPropertyNames||function(n){var e=[];for(var t in n)Object.prototype.hasOwnProperty.call(n,t)&&(e[e.length]=t);return e},n(e)};return function(e){if(e&&e.__esModule)return e;var t={};if(null!=e)for(var a=n(e),o=0;o<a.length;o++)"default"!==a[o]&&__createBinding(t,e,a[o]);return __setModuleDefault(t,e),t}}();Object.defineProperty(exports,"__esModule",{value:!0}),exports.NexusGraphProvider=void 0;const vscode=__importStar(require("vscode")),NEXUS_CONFIG={themes:{cosmic:{nodeColors:["#00d4ff","#ff00ff","#00ff88","#ffaa00","#ff0066"],edgeGlow:!0,particles:!0,gradients:!0},matrix:{nodeColors:["#00ff41","#39ff14","#20c020"],edgeGlow:!0,particles:!1,gradients:!1},sunset:{nodeColors:["#ff6b6b","#feca57","#ff9ff3","#ff6348"],edgeGlow:!1,particles:!0,gradients:!0}},animations:{smooth:{duration:800,easing:"ease-in-out"},bouncy:{duration:1200,easing:"cubic-bezier(0.68, -0.55, 0.265, 1.55)"},elastic:{duration:1e3,easing:"cubic-bezier(0.175, 0.885, 0.32, 1.275)"}},layouts:{galaxy:{spread:200,gravity:-50,damping:.95},nebula:{spread:300,gravity:-30,damping:.98},cluster:{spread:150,gravity:-80,damping:.9},orbit:{spread:250,gravity:-100,damping:.85}}};class NexusGraphProvider{_extensionUri;api;static viewType="localhubGraphView";_view;_currentTheme="cosmic";_animationPreset="smooth";constructor(n,e){this._extensionUri=n,this.api=e}resolveWebviewView(n,e,t){this._view=n,n.webview.options={enableScripts:!0,localResourceRoots:[vscode.Uri.joinPath(this._extensionUri,"resources")]},n.webview.html=this._getNexusHtml(n.webview),this._setupMessageHandler(n),setTimeout(()=>{this._loadGraph(30,2)},100)}_setupMessageHandler(n){n.webview.onDidReceiveMessage(async n=>{switch(n.type){case"loadGraph":await this._loadGraph(n.days||30,n.minCochanges||2);break;case"openFile":await this._openFile(n.filePath);break;case"getFileHistory":try{const e=await this.api.getHistory(n.path,30);this._view&&this._view.webview.postMessage({type:"fileHistory",path:n.path,history:e.history||[]})}catch(e){this._view&&this._view.webview.postMessage({type:"fileHistory",path:n.path,history:[],error:String(e)})}break;case"showDiff":vscode.commands.executeCommand("localhub.showDiff",{filePath:n.path,version:n.version});break;case"changeTheme":this._currentTheme=n.theme,await this._refreshTheme();break;case"runSimulation":await this._runSimulation(n.preset)}})}async _loadGraph(n,e){if(this._view)try{const t=await this.api.getFileRelationships(n,e),a=this._enhanceGraphData(t);this._view.webview.postMessage({type:"updateGraph",data:a,theme:this._currentTheme,animation:this._animationPreset})}catch(n){this._view.webview.postMessage({type:"error",message:`Failed to load: ${n}`})}}_enhanceGraphData(n){return n.nodes&&(n.nodes=n.nodes.map(n=>({...n,mass:10*Math.log(n.size+1),charge:-300,heat:Math.random(),velocity:{x:0,y:0},force:{x:0,y:0}}))),n.edges&&(n.edges=n.edges.map(n=>({...n,springLength:100/(n.weight||1),springStrength:.1*n.weight}))),n}async _refreshTheme(){this._view&&this._view.webview.postMessage({type:"changeTheme",theme:NEXUS_CONFIG.themes[this._currentTheme]})}async _runSimulation(n){this._view&&this._view.webview.postMessage({type:"runSimulation",config:NEXUS_CONFIG.layouts[n]})}async _openFile(n){try{const e=await vscode.workspace.openTextDocument(vscode.Uri.file(n));await vscode.window.showTextDocument(e);try{await vscode.commands.executeCommand("revealInExplorer",vscode.Uri.file(n))}catch{}}catch{}}refresh(){this._loadGraph(30,2)}setFilter(n){this._view&&this._view.webview.postMessage({type:"setFilter",filter:n})}openFullscreen(){this._view&&this._view.webview.postMessage({type:"openFullscreen"})}getNexusHtmlContent(n){return this._getNexusHtml(n)}_fullTabPanel;openFullTab(){if(this._fullTabPanel)return void this._fullTabPanel.reveal(vscode.ViewColumn.One);const n=vscode.window.createWebviewPanel("localhubNexusGraphFull","🌌 Nexus Relationships",vscode.ViewColumn.One,{enableScripts:!0,retainContextWhenHidden:!0,localResourceRoots:[vscode.Uri.joinPath(this._extensionUri,"resources")]});this._fullTabPanel=n;let e=this._getNexusHtml(n.webview);e=e.replace("</body>",'\n<div id="classicNavBtn" style="position:fixed;bottom:12px;right:12px;z-index:10000;">\n    <button onclick="vsc.postMessage({type:\'openClassic\'})" style="\n        padding:8px 16px;background:linear-gradient(135deg,#00d4ff,#0078d4);\n        color:#fff;border:none;border-radius:8px;cursor:pointer;font-size:12px;\n        box-shadow:0 2px 8px rgba(0,212,255,0.4);transition:all 0.2s;\n    " onmouseover="this.style.transform=\'scale(1.05)\'" onmouseout="this.style.transform=\'scale(1)\'">\n        📈 Classic View\n    </button>\n</div>\n</body>'),n.webview.html=e,n.webview.onDidReceiveMessage(async e=>{switch(e.type){case"loadGraph":await this._loadGraphToPanel(n,e.days||30,e.minCochanges||2);break;case"openFile":await this._openFile(e.filePath);break;case"getFileHistory":try{const t=await this.api.getHistory(e.path,30);n.webview.postMessage({type:"fileHistory",path:e.path,history:t.history||[]})}catch(t){n.webview.postMessage({type:"fileHistory",path:e.path,history:[],error:String(t)})}break;case"showDiff":vscode.commands.executeCommand("localhub.showDiff",{filePath:e.path,version:e.version});break;case"changeTheme":this._currentTheme=e.theme,n.webview.postMessage({type:"changeTheme",theme:NEXUS_CONFIG.themes[e.theme]});break;case"runSimulation":n.webview.postMessage({type:"runSimulation",config:NEXUS_CONFIG.layouts[e.preset]});break;case"openClassic":vscode.commands.executeCommand("localhub.openGraphFullTab")}}),this._loadGraphToPanel(n,30,2),n.onDidDispose(()=>{this._fullTabPanel=void 0})}async _loadGraphToPanel(n,e,t){try{const a=await this.api.getFileRelationships(e,t),o=this._enhanceGraphData(a);n.webview.postMessage({type:"updateGraph",data:o,theme:this._currentTheme,animation:this._animationPreset})}catch(e){n.webview.postMessage({type:"error",message:`Failed to load: ${e}`})}}_getNexusHtml(n){const e=n.asWebviewUri(vscode.Uri.joinPath(this._extensionUri,"resources","cytoscape.min.js"));return`<!DOCTYPE html>\n<html>\n<head>\n<meta charset="UTF-8">\n<meta name="viewport" content="width=device-width, initial-scale=1.0">\n<style>\n${this._getNexusStyles()}\n</style>\n</head>\n<body>\n<div class="nexus-container">\n    ${this._getControlPanel()}\n    ${this._getCanvasArea()}\n    ${this._getListPanel()}\n    ${this._getEffectsOverlay()}\n</div>\n<script src="${e}"><\/script>\n<script>\n${this._getNexusScript()}\n<\/script>\n</body>\n</html>`}_getControlPanel(){return'\n<div class="control-panel">\n    <div class="control-section">\n        <label class="control-label">🎨 Theme</label>\n        <div class="theme-selector">\n            <button class="theme-btn active" data-theme="cosmic">🌌 Cosmic</button>\n            <button class="theme-btn" data-theme="matrix">💚 Matrix</button>\n            <button class="theme-btn" data-theme="sunset">🌅 Sunset</button>\n        </div>\n    </div>\n    \n    <div class="control-section">\n        <label class="control-label">⚡ Layout</label>\n        <div class="layout-selector">\n            <button class="layout-btn" data-layout="galaxy">🌀 Galaxy</button>\n            <button class="layout-btn" data-layout="nebula">✨ Nebula</button>\n            <button class="layout-btn" data-layout="cluster">📦 Cluster</button>\n            <button class="layout-btn active" data-layout="orbit">🪐 Orbit</button>\n        </div>\n    </div>\n    \n    <div class="control-section">\n        <label class="control-label">🎭 View Mode</label>\n        <div class="view-toggle">\n            <button id="viewGraph" class="view-btn active">🌐 Graph</button>\n            <button id="viewList" class="view-btn">📊 Analytics</button>\n            <button id="viewTime" class="view-btn">⏰ Timeline</button>\n        </div>\n    </div>\n    \n    <div class="control-section effects">\n        <button class="effect-btn" data-effect="pulse">💫 Pulse</button>\n        <button class="effect-btn" data-effect="wave">🌊 Wave</button>\n        <button class="effect-btn" data-effect="explode">💥 Explode</button>\n    </div>\n</div>'}_getCanvasArea(){return'\n<div class="canvas-area">\n    <canvas id="particleCanvas" class="particle-layer"></canvas>\n    <div id="cy" class="graph-layer"></div>\n    <div class="heat-map-overlay"></div>\n    <div class="stats-overlay">\n        <div class="stat-card">\n            <span class="stat-icon">🌟</span>\n            <span class="stat-value" id="nodeCount">0</span>\n            <span class="stat-label">nodes</span>\n        </div>\n        <div class="stat-card">\n            <span class="stat-icon">🔗</span>\n            <span class="stat-value" id="edgeCount">0</span>\n            <span class="stat-label">links</span>\n        </div>\n        <div class="stat-card">\n            <span class="stat-icon">⚡</span>\n            <span class="stat-value" id="energy">0</span>\n            <span class="stat-label">energy</span>\n        </div>\n    </div>\n</div>'}_getListPanel(){return'\n<div id="listPanel" class="list-panel">\n    <div class="list-header">\n        <h2>🔮 Nexus Analytics</h2>\n        <input type="text" placeholder="Search cosmos..." class="search-box" id="searchBox">\n    </div>\n    \n    <div class="cluster-container" id="clusterContainer">\n        \x3c!-- Dynamic clusters will be inserted here --\x3e\n    </div>\n    \n    <div class="timeline-container" id="timelineContainer">\n        <div class="tl-toolbar">\n            <span class="tl-title" id="tlTitle">⏱ File Activity Timeline</span>\n            <div class="tl-controls">\n                <button class="tl-btn" onclick="tlZoom(-1)" title="Zoom out">−</button>\n                <span class="tl-zoom-label" id="tlZoomLabel">100%</span>\n                <button class="tl-btn" onclick="tlZoom(1)" title="Zoom in">+</button>\n                <button class="tl-btn tl-fit" onclick="tlFitAll()" title="Fit all">⊞ Fit</button>\n            </div>\n        </div>\n        <div class="tl-axis" id="tlAxis"></div>\n        <div class="tl-body" id="tlBody">\n            <div class="tl-scroll" id="tlScroll"></div>\n        </div>\n        <div class="tl-tooltip" id="tlTooltip"></div>\n    </div>\n</div>'}_getEffectsOverlay(){return'\n<div class="effects-overlay">\n    <svg class="glow-filters">\n        <defs>\n            <filter id="neon-glow">\n                <feGaussianBlur stdDeviation="4" result="coloredBlur"/>\n                <feMerge>\n                    <feMergeNode in="coloredBlur"/>\n                    <feMergeNode in="coloredBlur"/>\n                    <feMergeNode in="SourceGraphic"/>\n                </feMerge>\n            </filter>\n        </defs>\n    </svg>\n</div>'}_getNexusStyles(){return"\n/* 🎨 NEXUS VISION ULTIMATE STYLES */\n\n* { margin: 0; padding: 0; box-sizing: border-box; }\n\nbody {\n    background: linear-gradient(135deg, #0a0e27 0%, #151932 100%);\n    color: #e0e6ed;\n    font-family: 'Segoe UI', system-ui, sans-serif;\n    overflow: hidden;\n}\n\n.nexus-container {\n    width: 100vw;\n    height: 100vh;\n    display: flex;\n    flex-direction: column;\n}\n\n/* Control Panel */\n.control-panel {\n    padding: 12px;\n    background: rgba(10, 14, 39, 0.8);\n    backdrop-filter: blur(10px);\n    border-bottom: 1px solid rgba(255,255,255,0.1);\n    display: flex;\n    gap: 20px;\n    align-items: center;\n    position: relative;\n    z-index: 1000;\n    flex-wrap: wrap;\n}\n\n.control-section {\n    display: flex;\n    align-items: center;\n    gap: 10px;\n}\n\n.control-label {\n    font-size: 11px;\n    text-transform: uppercase;\n    letter-spacing: 1px;\n    opacity: 0.7;\n}\n\n/* Theme & Layout buttons */\n.theme-selector, .layout-selector, .view-toggle {\n    display: flex;\n    gap: 4px;\n    background: rgba(255,255,255,0.05);\n    padding: 3px;\n    border-radius: 20px;\n}\n\n.theme-btn, .layout-btn, .view-btn, .effect-btn {\n    padding: 6px 12px;\n    border-radius: 16px;\n    border: none;\n    background: transparent;\n    color: #8b92a8;\n    font-size: 12px;\n    cursor: pointer;\n    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);\n    position: relative;\n    overflow: hidden;\n}\n\n.theme-btn::before, .layout-btn::before, .view-btn::before {\n    content: '';\n    position: absolute;\n    top: 50%;\n    left: 50%;\n    width: 0;\n    height: 0;\n    border-radius: 50%;\n    background: rgba(255,255,255,0.2);\n    transform: translate(-50%, -50%);\n    transition: width 0.6s, height 0.6s;\n}\n\n.theme-btn:hover::before, .layout-btn:hover::before, .view-btn:hover::before {\n    width: 100px;\n    height: 100px;\n}\n\n.theme-btn.active, .layout-btn.active, .view-btn.active {\n    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);\n    color: white;\n    box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);\n}\n\n.effect-btn {\n    background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);\n    color: white;\n}\n\n.effect-btn:hover {\n    transform: translateY(-2px);\n    box-shadow: 0 6px 20px rgba(240, 147, 251, 0.5);\n}\n\n/* Canvas Area */\n.canvas-area {\n    flex: 1;\n    position: relative;\n    overflow: hidden;\n    background: radial-gradient(ellipse at center, rgba(102, 126, 234, 0.05) 0%, transparent 70%);\n}\n\n.particle-layer {\n    position: absolute;\n    top: 0;\n    left: 0;\n    width: 100%;\n    height: 100%;\n    pointer-events: none;\n    opacity: 0.6;\n}\n\n.graph-layer {\n    position: absolute;\n    top: 0;\n    left: 0;\n    width: 100%;\n    height: 100%;\n}\n\n.heat-map-overlay {\n    position: absolute;\n    top: 0;\n    left: 0;\n    width: 100%;\n    height: 100%;\n    pointer-events: none;\n    mix-blend-mode: screen;\n    opacity: 0;\n    transition: opacity 0.5s;\n}\n\n.heat-map-overlay.active {\n    opacity: 0.3;\n    background: radial-gradient(\n        circle at var(--mouse-x) var(--mouse-y),\n        rgba(255, 100, 100, 0.3) 0%,\n        transparent 30%\n    );\n}\n\n/* Stats Overlay */\n.stats-overlay {\n    position: absolute;\n    top: 20px;\n    right: 20px;\n    display: flex;\n    gap: 15px;\n}\n\n.stat-card {\n    background: rgba(10, 14, 39, 0.9);\n    backdrop-filter: blur(10px);\n    padding: 10px 15px;\n    border-radius: 12px;\n    border: 1px solid rgba(255,255,255,0.1);\n    display: flex;\n    align-items: center;\n    gap: 8px;\n    animation: slideIn 0.6s ease;\n}\n\n@keyframes slideIn {\n    from {\n        opacity: 0;\n        transform: translateY(-20px);\n    }\n    to {\n        opacity: 1;\n        transform: translateY(0);\n    }\n}\n\n.stat-icon {\n    font-size: 18px;\n}\n\n.stat-value {\n    font-size: 20px;\n    font-weight: 700;\n    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);\n    -webkit-background-clip: text;\n    -webkit-text-fill-color: transparent;\n}\n\n.stat-label {\n    font-size: 11px;\n    text-transform: uppercase;\n    opacity: 0.6;\n}\n\n/* List Panel */\n.list-panel {\n    position: absolute;\n    left: 0;\n    top: 0;\n    width: 100%;\n    height: 100%;\n    background: rgba(10, 14, 39, 0.95);\n    backdrop-filter: blur(20px);\n    transform: translateX(-100%);\n    transition: transform 0.5s cubic-bezier(0.4, 0, 0.2, 1);\n    overflow-y: auto;\n    padding: 30px;\n}\n\n.list-panel.show {\n    transform: translateX(0);\n}\n\n.list-header {\n    margin-bottom: 30px;\n}\n\n.list-header h2 {\n    font-size: 24px;\n    margin-bottom: 15px;\n    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);\n    -webkit-background-clip: text;\n    -webkit-text-fill-color: transparent;\n}\n\n.search-box {\n    width: 100%;\n    padding: 12px 20px;\n    background: rgba(255,255,255,0.05);\n    border: 1px solid rgba(255,255,255,0.1);\n    border-radius: 25px;\n    color: white;\n    font-size: 14px;\n}\n\n.search-box::placeholder {\n    color: rgba(255,255,255,0.3);\n}\n\n.search-box:focus {\n    outline: none;\n    border-color: #667eea;\n    box-shadow: 0 0 20px rgba(102, 126, 234, 0.3);\n}\n\n/* Cluster Cards */\n.cluster-container {\n    display: grid;\n    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));\n    gap: 20px;\n}\n\n.cluster-card {\n    background: linear-gradient(135deg, rgba(102, 126, 234, 0.1) 0%, rgba(118, 75, 162, 0.1) 100%);\n    border: 1px solid rgba(255,255,255,0.1);\n    border-radius: 16px;\n    padding: 20px;\n    cursor: pointer;\n    transition: all 0.3s;\n    position: relative;\n    overflow: hidden;\n}\n\n.cluster-card::before {\n    content: '';\n    position: absolute;\n    top: -2px;\n    left: -2px;\n    right: -2px;\n    bottom: -2px;\n    background: linear-gradient(45deg, #667eea, #764ba2, #f093fb, #f5576c);\n    border-radius: 16px;\n    z-index: -1;\n    opacity: 0;\n    transition: opacity 0.3s;\n}\n\n.cluster-card:hover::before {\n    opacity: 1;\n    animation: gradient-rotate 3s linear infinite;\n}\n\n@keyframes gradient-rotate {\n    0% { transform: rotate(0deg); }\n    100% { transform: rotate(360deg); }\n}\n\n.cluster-card:hover {\n    transform: translateY(-5px);\n    box-shadow: 0 10px 40px rgba(102, 126, 234, 0.3);\n}\n\n.cluster-title {\n    font-size: 16px;\n    font-weight: 600;\n    margin-bottom: 10px;\n}\n\n.cluster-stats {\n    display: flex;\n    gap: 15px;\n    margin-top: 15px;\n}\n\n.cluster-stat {\n    display: flex;\n    align-items: center;\n    gap: 5px;\n    font-size: 12px;\n    opacity: 0.8;\n}\n\n/* Timeline */\n.timeline-container {\n    flex-direction: column;\n    height: 100%;\n    background: rgba(5, 8, 22, 0.95);\n    position: relative;\n    overflow: hidden;\n}\n\n.tl-toolbar {\n    display: flex;\n    justify-content: space-between;\n    align-items: center;\n    padding: 10px 16px;\n    background: rgba(15, 20, 50, 0.9);\n    border-bottom: 1px solid rgba(100, 160, 255, 0.15);\n    flex-shrink: 0;\n}\n.tl-title {\n    font-size: 13px;\n    font-weight: 600;\n    color: rgba(140, 190, 255, 0.9);\n    letter-spacing: 0.5px;\n}\n.tl-controls {\n    display: flex;\n    align-items: center;\n    gap: 6px;\n}\n.tl-btn {\n    background: rgba(80, 130, 255, 0.15);\n    border: 1px solid rgba(80, 130, 255, 0.25);\n    color: rgba(160, 200, 255, 0.9);\n    border-radius: 6px;\n    padding: 4px 10px;\n    cursor: pointer;\n    font-size: 13px;\n    font-weight: 600;\n    transition: all 0.2s;\n}\n.tl-btn:hover {\n    background: rgba(80, 130, 255, 0.3);\n    border-color: rgba(80, 130, 255, 0.5);\n    color: #fff;\n}\n.tl-zoom-label {\n    font-size: 11px;\n    color: rgba(160, 200, 255, 0.6);\n    min-width: 36px;\n    text-align: center;\n}\n\n/* Time axis (sticky top) */\n.tl-axis {\n    display: flex;\n    padding: 6px 16px 6px 180px;\n    background: rgba(10, 15, 40, 0.85);\n    border-bottom: 1px solid rgba(100, 160, 255, 0.1);\n    flex-shrink: 0;\n    overflow: hidden;\n    position: relative;\n    min-height: 24px;\n}\n.tl-axis-label {\n    position: absolute;\n    font-size: 10px;\n    color: rgba(120, 170, 255, 0.55);\n    white-space: nowrap;\n    letter-spacing: 0.3px;\n    top: 6px;\n}\n.tl-axis-tick {\n    position: absolute;\n    top: 20px;\n    width: 1px;\n    height: 6px;\n    background: rgba(100, 160, 255, 0.2);\n}\n\n/* Scrollable file rows */\n.tl-body {\n    flex: 1;\n    overflow-y: auto;\n    overflow-x: hidden;\n    padding-bottom: 20px;\n}\n.tl-body::-webkit-scrollbar { width: 6px; }\n.tl-body::-webkit-scrollbar-track { background: rgba(255,255,255,0.02); }\n.tl-body::-webkit-scrollbar-thumb { background: rgba(100,160,255,0.2); border-radius: 3px; }\n\n/* Folder header */\n.tl-folder {\n    display: flex;\n    align-items: center;\n    padding: 8px 16px;\n    background: rgba(20, 30, 70, 0.5);\n    border-top: 1px solid rgba(100, 160, 255, 0.08);\n    cursor: pointer;\n    user-select: none;\n}\n.tl-folder:hover { background: rgba(30, 45, 90, 0.6); }\n.tl-folder-icon {\n    font-size: 13px;\n    margin-right: 6px;\n    transition: transform 0.2s;\n}\n.tl-folder.collapsed .tl-folder-icon { transform: rotate(-90deg); }\n.tl-folder-name {\n    font-size: 12px;\n    font-weight: 600;\n    color: rgba(150, 195, 255, 0.85);\n    letter-spacing: 0.3px;\n}\n.tl-folder-count {\n    margin-left: 8px;\n    font-size: 10px;\n    color: rgba(120, 170, 255, 0.4);\n}\n\n/* File row */\n.tl-row {\n    display: flex;\n    align-items: center;\n    height: 32px;\n    padding: 0 16px;\n    border-bottom: 1px solid rgba(255,255,255,0.03);\n    transition: background 0.15s;\n}\n.tl-row:hover {\n    background: rgba(60, 100, 200, 0.1);\n}\n.tl-row-name {\n    width: 164px;\n    min-width: 164px;\n    font-size: 11px;\n    color: rgba(200, 220, 255, 0.75);\n    white-space: nowrap;\n    overflow: hidden;\n    text-overflow: ellipsis;\n    padding-right: 12px;\n}\n.tl-row-ext {\n    color: rgba(100, 160, 255, 0.5);\n}\n.tl-row-bar {\n    flex: 1;\n    height: 100%;\n    position: relative;\n    overflow: hidden;\n}\n\n/* Activity dot on timeline */\n.tl-dot {\n    position: absolute;\n    top: 50%;\n    transform: translate(-50%, -50%);\n    border-radius: 50%;\n    cursor: pointer;\n    transition: all 0.2s;\n}\n.tl-dot::before {\n    content: '';\n    position: absolute;\n    top: 50%;\n    left: 50%;\n    transform: translate(-50%, -50%);\n    border-radius: 50%;\n    background: inherit;\n    opacity: 0.25;\n    filter: blur(4px);\n}\n.tl-dot:hover {\n    transform: translate(-50%, -50%) scale(1.6);\n    z-index: 10;\n    box-shadow: 0 0 12px rgba(88, 166, 255, 0.6);\n}\n\n/* Gridlines */\n.tl-gridline {\n    position: absolute;\n    top: 0;\n    width: 1px;\n    height: 100%;\n    background: rgba(100, 160, 255, 0.06);\n    pointer-events: none;\n}\n\n/* Tooltip */\n.tl-tooltip {\n    display: none;\n    position: absolute;\n    background: rgba(15, 20, 50, 0.95);\n    border: 1px solid rgba(100, 160, 255, 0.3);\n    border-radius: 8px;\n    padding: 10px 14px;\n    font-size: 11px;\n    color: rgba(200, 225, 255, 0.9);\n    z-index: 100;\n    pointer-events: none;\n    backdrop-filter: blur(10px);\n    box-shadow: 0 4px 20px rgba(0,0,0,0.5), 0 0 15px rgba(80, 130, 255, 0.15);\n    max-width: 340px;\n    line-height: 1.5;\n}\n.tl-tooltip.show { display: block; }\n.tl-tooltip-path {\n    color: rgba(100, 160, 255, 0.7);\n    font-size: 10px;\n    margin-bottom: 4px;\n    word-break: break-all;\n}\n.tl-tooltip-date {\n    color: rgba(80, 200, 120, 0.9);\n    font-weight: 600;\n}\n.tl-tooltip-size {\n    color: rgba(255, 180, 80, 0.9);\n    margin-left: 10px;\n}\n\n/* Active row highlight */\n.tl-row-active {\n    background: rgba(60, 100, 220, 0.15) !important;\n    border-left: 2px solid rgba(88, 166, 255, 0.6);\n}\n\n/* Expandable detail panel (appears under clicked row) */\n.tl-detail {\n    background: rgba(12, 18, 45, 0.95);\n    border: 1px solid rgba(80, 130, 255, 0.2);\n    border-radius: 8px;\n    margin: 4px 16px 8px 16px;\n    overflow: hidden;\n    animation: tlDetailSlide 0.2s ease-out;\n}\n@keyframes tlDetailSlide {\n    from { opacity: 0; max-height: 0; }\n    to { opacity: 1; max-height: 500px; }\n}\n.tl-detail-header {\n    display: flex;\n    justify-content: space-between;\n    align-items: center;\n    padding: 8px 14px;\n    background: rgba(20, 30, 65, 0.8);\n    border-bottom: 1px solid rgba(80, 130, 255, 0.12);\n}\n.tl-detail-title {\n    font-size: 12px;\n    font-weight: 600;\n    color: rgba(140, 190, 255, 0.9);\n}\n.tl-detail-close {\n    background: none;\n    border: none;\n    color: rgba(200, 200, 255, 0.5);\n    cursor: pointer;\n    font-size: 14px;\n    padding: 2px 6px;\n    border-radius: 4px;\n}\n.tl-detail-close:hover {\n    background: rgba(255, 80, 80, 0.2);\n    color: #f66;\n}\n.tl-detail-loading, .tl-detail-empty {\n    padding: 16px;\n    text-align: center;\n    color: rgba(160, 200, 255, 0.5);\n    font-size: 12px;\n}\n.tl-detail-versions {\n    max-height: 240px;\n    overflow-y: auto;\n}\n.tl-detail-versions::-webkit-scrollbar { width: 4px; }\n.tl-detail-versions::-webkit-scrollbar-thumb { background: rgba(100,160,255,0.2); border-radius: 2px; }\n\n/* Version row */\n.tl-ver-row {\n    display: flex;\n    align-items: center;\n    gap: 10px;\n    padding: 6px 14px;\n    border-bottom: 1px solid rgba(255,255,255,0.03);\n    cursor: pointer;\n    transition: background 0.15s;\n    font-size: 11px;\n}\n.tl-ver-row:hover {\n    background: rgba(60, 100, 200, 0.15);\n}\n.tl-ver-latest {\n    background: rgba(40, 80, 180, 0.1);\n}\n.tl-ver-num {\n    min-width: 32px;\n    font-weight: 700;\n    color: rgba(100, 180, 255, 0.9);\n    font-size: 11px;\n}\n.tl-ver-date {\n    min-width: 140px;\n    color: rgba(80, 200, 120, 0.8);\n    font-size: 10px;\n}\n.tl-ver-size {\n    min-width: 55px;\n    color: rgba(200, 180, 150, 0.6);\n    font-size: 10px;\n    text-align: right;\n}\n.tl-ver-reason {\n    flex: 1;\n    color: rgba(200, 200, 255, 0.55);\n    font-size: 10px;\n    white-space: nowrap;\n    overflow: hidden;\n    text-overflow: ellipsis;\n    max-width: 200px;\n}\n.tl-ver-action {\n    color: rgba(88, 166, 255, 0.7);\n    font-size: 10px;\n    opacity: 0;\n    transition: opacity 0.15s;\n}\n.tl-ver-row:hover .tl-ver-action {\n    opacity: 1;\n}\n\n/* Animations */\n@keyframes pulse {\n    0%, 100% { transform: scale(1); opacity: 1; }\n    50% { transform: scale(1.1); opacity: 0.8; }\n}\n\n@keyframes wave {\n    0% { transform: translateX(0); }\n    100% { transform: translateX(100%); }\n}\n\n.pulse-effect {\n    animation: pulse 2s infinite;\n}\n\n.wave-effect {\n    position: absolute;\n    top: 0;\n    left: -100%;\n    width: 100%;\n    height: 100%;\n    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);\n    animation: wave 2s linear infinite;\n}\n\n/* Glow effects */\n.neon-node {\n    filter: url(#neon-glow);\n}\n\n/* Scrollbar */\n::-webkit-scrollbar {\n    width: 8px;\n}\n\n::-webkit-scrollbar-track {\n    background: rgba(255,255,255,0.05);\n    border-radius: 4px;\n}\n\n::-webkit-scrollbar-thumb {\n    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);\n    border-radius: 4px;\n}\n"}_getNexusScript(){return"\n// 🚀 NEXUS VISION ULTIMATE CONTROLLER\n\nconst vsc = acquireVsCodeApi();\nlet cy;\nlet currentTheme = 'cosmic';\nlet currentLayout = 'orbit';\nlet particles = [];\nlet animationFrame;\nlet lastGraphData = null;\n\n// ═══════════════════════════════════════════════════════════════\n// PARTICLE SYSTEM\n// ═══════════════════════════════════════════════════════════════\n\nclass Particle {\n    constructor(canvas) {\n        this.canvas = canvas;\n        this.ctx = canvas.getContext('2d');\n        this.reset();\n    }\n    \n    reset() {\n        this.x = Math.random() * this.canvas.width;\n        this.y = Math.random() * this.canvas.height;\n        this.vx = (Math.random() - 0.5) * 0.5;\n        this.vy = (Math.random() - 0.5) * 0.5;\n        this.size = Math.random() * 2 + 0.5;\n        this.life = 1;\n        this.decay = Math.random() * 0.01 + 0.005;\n        this.hue = Math.random() * 60 + 200;\n    }\n    \n    update() {\n        this.x += this.vx;\n        this.y += this.vy;\n        this.life -= this.decay;\n        \n        if (this.life <= 0 || this.x < 0 || this.x > this.canvas.width || \n            this.y < 0 || this.y > this.canvas.height) {\n            this.reset();\n        }\n    }\n    \n    draw() {\n        this.ctx.save();\n        this.ctx.globalAlpha = this.life;\n        this.ctx.fillStyle = `hsl(${this.hue}, 100%, 60%)`;\n        this.ctx.shadowBlur = 10;\n        this.ctx.shadowColor = `hsl(${this.hue}, 100%, 60%)`;\n        this.ctx.beginPath();\n        this.ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);\n        this.ctx.fill();\n        this.ctx.restore();\n    }\n}\n\nfunction initParticles() {\n    const canvas = document.getElementById('particleCanvas');\n    canvas.width = window.innerWidth;\n    canvas.height = window.innerHeight;\n    \n    for (let i = 0; i < 50; i++) {\n        particles.push(new Particle(canvas));\n    }\n    \n    animateParticles();\n}\n\nfunction animateParticles() {\n    const canvas = document.getElementById('particleCanvas');\n    const ctx = canvas.getContext('2d');\n    \n    ctx.fillStyle = 'rgba(10, 14, 39, 0.1)';\n    ctx.fillRect(0, 0, canvas.width, canvas.height);\n    \n    particles.forEach(p => {\n        p.update();\n        p.draw();\n    });\n    \n    animationFrame = requestAnimationFrame(animateParticles);\n}\n\n// ═══════════════════════════════════════════════════════════════\n// CYTOSCAPE SETUP\n// ═══════════════════════════════════════════════════════════════\n\nfunction initCytoscape() {\n    const container = document.getElementById('cy');\n    \n    cy = cytoscape({\n        container: container,\n        style: getCytoscapeStyle(),\n        layout: { name: 'preset' }\n    });\n    \n    // Mouse tracking for heat map\n    container.addEventListener('mousemove', (e) => {\n        const rect = container.getBoundingClientRect();\n        document.querySelector('.heat-map-overlay').style.setProperty('--mouse-x', `${e.clientX - rect.left}px`);\n        document.querySelector('.heat-map-overlay').style.setProperty('--mouse-y', `${e.clientY - rect.top}px`);\n    });\n    \n    // Double click to open file\n    cy.on('dbltap', 'node', (evt) => {\n        const node = evt.target;\n        vsc.postMessage({ \n            type: 'openFile', \n            filePath: node.data('fullPath') \n        });\n    });\n    \n    // Hover effects\n    cy.on('mouseover', 'node', (evt) => {\n        evt.target.addClass('hover');\n        document.querySelector('.heat-map-overlay').classList.add('active');\n    });\n    \n    cy.on('mouseout', 'node', (evt) => {\n        evt.target.removeClass('hover');\n        document.querySelector('.heat-map-overlay').classList.remove('active');\n    });\n}\n\nfunction getCytoscapeStyle() {\n    const themes = {\n        cosmic: {\n            node: '#00d4ff',\n            edge: 'rgba(102, 126, 234, 0.4)',\n            selected: '#ff00ff',\n            label: '#e0e6ed'\n        },\n        matrix: {\n            node: '#00ff41',\n            edge: 'rgba(0, 255, 65, 0.3)',\n            selected: '#39ff14',\n            label: '#00ff41'\n        },\n        sunset: {\n            node: '#ff6b6b',\n            edge: 'rgba(254, 202, 87, 0.4)',\n            selected: '#feca57',\n            label: '#fff'\n        }\n    };\n    \n    const theme = themes[currentTheme];\n    \n    return [\n        {\n            selector: 'node',\n            style: {\n                'background-color': theme.node,\n                'label': 'data(label)',\n                'font-size': '10px',\n                'width': 'mapData(size, 0, 100, 20, 80)',\n                'height': 'mapData(size, 0, 100, 20, 80)',\n                'color': theme.label,\n                'text-outline-color': '#0a0e27',\n                'text-outline-width': 2,\n                'border-width': 0,\n                'transition-property': 'background-color, width, height',\n                'transition-duration': '0.3s'\n            }\n        },\n        {\n            selector: 'node.hover',\n            style: {\n                'background-color': theme.selected,\n                'width': 'mapData(size, 0, 100, 30, 100)',\n                'height': 'mapData(size, 0, 100, 30, 100)',\n                'z-index': 999\n            }\n        },\n        {\n            selector: 'edge',\n            style: {\n                'width': 'mapData(weight, 0, 10, 1, 5)',\n                'line-color': theme.edge,\n                'curve-style': 'bezier',\n                'target-arrow-shape': 'none',\n                'opacity': 0.6,\n                'transition-property': 'line-color, width',\n                'transition-duration': '0.3s'\n            }\n        },\n        {\n            selector: 'edge[type=\"temporal\"]',\n            style: {\n                'line-style': 'dashed',\n                'line-dash-pattern': [6, 3]\n            }\n        },\n        {\n            selector: '.highlighted',\n            style: {\n                'background-color': theme.selected,\n                'line-color': theme.selected,\n                'opacity': 1\n            }\n        }\n    ];\n}\n\n// ═══════════════════════════════════════════════════════════════\n// PHYSICS ENGINE\n// ═══════════════════════════════════════════════════════════════\n\nclass PhysicsEngine {\n    constructor(nodes, edges, config) {\n        this.nodes = nodes;\n        this.edges = edges;\n        this.config = config;\n        this.running = false;\n        this.energy = 100;\n    }\n    \n    start() {\n        this.running = true;\n        this.simulate();\n    }\n    \n    stop() {\n        this.running = false;\n    }\n    \n    simulate() {\n        if (!this.running) return;\n        \n        // Apply forces\n        this.applyGravity();\n        this.applyRepulsion();\n        this.applySprings();\n        \n        // Update positions\n        this.updatePositions();\n        \n        // Update energy display\n        document.getElementById('energy').textContent = Math.round(this.energy);\n        \n        // Damping\n        this.energy *= this.config.damping;\n        \n        if (this.energy > 0.1 && !document.hidden) {\n            requestAnimationFrame(() => this.simulate());\n        } else {\n            this.running = false;\n            if (cy && cy.elements().length > 0) cy.fit(cy.elements(), 40);\n        }\n    }\n    \n    applyGravity() {\n        const center = { x: cy.width() / 2, y: cy.height() / 2 };\n        \n        this.nodes.forEach(node => {\n            const pos = node.position();\n            const dx = center.x - pos.x;\n            const dy = center.y - pos.y;\n            const dist = Math.sqrt(dx * dx + dy * dy);\n            \n            if (dist > 0) {\n                const force = this.config.gravity / dist;\n                node.data('fx', (node.data('fx') || 0) + dx * force);\n                node.data('fy', (node.data('fy') || 0) + dy * force);\n            }\n        });\n    }\n    \n    applyRepulsion() {\n        for (let i = 0; i < this.nodes.length; i++) {\n            for (let j = i + 1; j < this.nodes.length; j++) {\n                const n1 = this.nodes[i];\n                const n2 = this.nodes[j];\n                const p1 = n1.position();\n                const p2 = n2.position();\n                \n                const dx = p2.x - p1.x;\n                const dy = p2.y - p1.y;\n                const dist = Math.sqrt(dx * dx + dy * dy) + 0.1;\n                \n                const force = (this.config.spread * this.config.spread) / dist;\n                \n                n1.data('fx', (n1.data('fx') || 0) - dx * force / dist);\n                n1.data('fy', (n1.data('fy') || 0) - dy * force / dist);\n                n2.data('fx', (n2.data('fx') || 0) + dx * force / dist);\n                n2.data('fy', (n2.data('fy') || 0) + dy * force / dist);\n            }\n        }\n    }\n    \n    applySprings() {\n        this.edges.forEach(edge => {\n            const source = edge.source();\n            const target = edge.target();\n            const p1 = source.position();\n            const p2 = target.position();\n            \n            const dx = p2.x - p1.x;\n            const dy = p2.y - p1.y;\n            const dist = Math.sqrt(dx * dx + dy * dy) + 0.1;\n            const springLength = edge.data('springLength') || 100;\n            const springStrength = edge.data('springStrength') || 0.1;\n            \n            const force = (dist - springLength) * springStrength;\n            \n            source.data('fx', (source.data('fx') || 0) + dx * force / dist);\n            source.data('fy', (source.data('fy') || 0) + dy * force / dist);\n            target.data('fx', (target.data('fx') || 0) - dx * force / dist);\n            target.data('fy', (target.data('fy') || 0) - dy * force / dist);\n        });\n    }\n    \n    updatePositions() {\n        let totalMovement = 0;\n        \n        this.nodes.forEach(node => {\n            const fx = node.data('fx') || 0;\n            const fy = node.data('fy') || 0;\n            const mass = node.data('mass') || 1;\n            \n            const ax = fx / mass;\n            const ay = fy / mass;\n            \n            const vx = (node.data('vx') || 0) * this.config.damping + ax;\n            const vy = (node.data('vy') || 0) * this.config.damping + ay;\n            \n            node.data('vx', vx);\n            node.data('vy', vy);\n            \n            const pos = node.position();\n            let newX = pos.x + vx;\n            let newY = pos.y + vy;\n\n            // Bounds clamping — keep nodes inside viewport\n            const w = cy.width();\n            const h = cy.height();\n            const pad = 30;\n            if (newX < pad) { newX = pad; node.data('vx', Math.abs(vx) * 0.3); }\n            if (newX > w - pad) { newX = w - pad; node.data('vx', -Math.abs(vx) * 0.3); }\n            if (newY < pad) { newY = pad; node.data('vy', Math.abs(vy) * 0.3); }\n            if (newY > h - pad) { newY = h - pad; node.data('vy', -Math.abs(vy) * 0.3); }\n\n            node.position({ x: newX, y: newY });\n            \n            // Reset forces\n            node.data('fx', 0);\n            node.data('fy', 0);\n            \n            totalMovement += Math.abs(vx) + Math.abs(vy);\n        });\n        \n        this.energy = Math.min(100, totalMovement / this.nodes.length);\n    }\n}\n\n// ═══════════════════════════════════════════════════════════════\n// GRAPH OPERATIONS\n// ═══════════════════════════════════════════════════════════════\n\nfunction updateGraph(data, theme, animation) {\n    lastGraphData = data;\n    if (!cy) initCytoscape();\n\n    // Clear existing\n    cy.elements().remove();\n\n    if (!data.nodes || data.nodes.length === 0) {\n        showEmptyState();\n        return;\n    }\n    \n    // Add nodes and edges\n    const elements = [];\n    \n    data.nodes.forEach(node => {\n        elements.push({\n            group: 'nodes',\n            data: {\n                id: node.id,\n                label: node.label,\n                fullPath: node.full_path,\n                size: node.size,\n                mass: node.mass,\n                fx: 0,\n                fy: 0,\n                vx: 0,\n                vy: 0\n            },\n            position: {\n                x: Math.random() * cy.width(),\n                y: Math.random() * cy.height()\n            }\n        });\n    });\n    \n    data.edges.forEach(edge => {\n        elements.push({\n            group: 'edges',\n            data: {\n                id: 'e' + edge.source + '-' + edge.target,\n                source: edge.source,\n                target: edge.target,\n                weight: edge.weight,\n                type: edge.type,\n                springLength: edge.springLength,\n                springStrength: edge.springStrength\n            }\n        });\n    });\n    \n    cy.add(elements);\n    \n    // Update stats\n    document.getElementById('nodeCount').textContent = data.nodes.length;\n    document.getElementById('edgeCount').textContent = data.edges.length;\n    \n    // Run physics simulation\n    runLayout(currentLayout);\n\n    // Fit graph to viewport after initial placement + after physics settles\n    setTimeout(() => { if (cy) cy.fit(cy.elements(), 40); }, 500);\n\n    // Update list panel\n    updateListPanel(data);\n}\n\nfunction runLayout(layoutName) {\n    if (!cy || cy.nodes().length === 0) return;\n\n    switch (layoutName) {\n        case 'galaxy':  layoutGalaxy();  break;\n        case 'nebula':  layoutNebula();  break;\n        case 'cluster': layoutCluster(); break;\n        case 'orbit':   layoutOrbit();   break;\n        default:        layoutNebula();  break;\n    }\n}\n\n// Energy decay animation (cosmetic — shows energy dropping to 0)\nfunction animateEnergyDecay() {\n    let e = 100;\n    const decay = setInterval(() => {\n        e *= 0.88;\n        document.getElementById('energy').textContent = Math.round(e);\n        if (e < 1) { clearInterval(decay); document.getElementById('energy').textContent = '0'; }\n    }, 80);\n}\n\n// ── GALAXY: Golden-angle spiral ──────────────────────────────\n// Most important/largest nodes near the center, spiraling outward.\nfunction layoutGalaxy() {\n    const nodes = cy.nodes().sort((a, b) => (b.data('size') || 1) - (a.data('size') || 1));\n    const centerX = cy.width() / 2;\n    const centerY = cy.height() / 2;\n    const goldenAngle = Math.PI * (3 - Math.sqrt(5)); // ~137.508 deg\n    const maxR = Math.min(centerX, centerY) * 0.85;\n    const spacing = maxR / Math.sqrt(nodes.length || 1);\n\n    nodes.forEach((node, i) => {\n        const angle = i * goldenAngle;\n        const r = spacing * Math.sqrt(i + 0.5);\n        node.animate(\n            { position: { x: centerX + r * Math.cos(angle), y: centerY + r * Math.sin(angle) } },\n            { duration: 800 }\n        );\n    });\n\n    animateEnergyDecay();\n    setTimeout(() => { if (cy) cy.fit(cy.elements(), 40); }, 900);\n}\n\n// ── NEBULA: Organic force-directed cloud ─────────────────────\n// Original PhysicsEngine — nodes float and settle naturally.\nfunction layoutNebula() {\n    const physics = new PhysicsEngine(\n        cy.nodes(), cy.edges(),\n        { spread: 300, gravity: -30, damping: 0.98 }\n    );\n    physics.start();\n}\n\n// ── CLUSTER: Grouped by connected components ─────────────────\n// Each connected component gets its own compact circle in a grid.\nfunction layoutCluster() {\n    const nodes = cy.nodes();\n    const visited = new Set();\n    const clusters = [];\n\n    // BFS to find connected components\n    nodes.forEach(node => {\n        if (visited.has(node.id())) return;\n        const component = [];\n        const stack = [node];\n        while (stack.length) {\n            const n = stack.pop();\n            if (visited.has(n.id())) continue;\n            visited.add(n.id());\n            component.push(n);\n            n.connectedEdges().forEach(edge => {\n                const other = edge.source().id() === n.id() ? edge.target() : edge.source();\n                if (!visited.has(other.id())) stack.push(other);\n            });\n        }\n        clusters.push(component);\n    });\n\n    clusters.sort((a, b) => b.length - a.length);\n\n    // Arrange clusters in a grid\n    const cols = Math.max(1, Math.ceil(Math.sqrt(clusters.length)));\n    const rows = Math.ceil(clusters.length / cols);\n    const cellW = cy.width() / (cols + 1);\n    const cellH = cy.height() / (rows + 1);\n\n    clusters.forEach((cluster, ci) => {\n        const col = ci % cols;\n        const row = Math.floor(ci / cols);\n        const cx = cellW * (col + 1);\n        const ccy = cellH * (row + 1);\n        const radius = Math.min(cellW, cellH) * 0.35 * Math.min(1, cluster.length / 6);\n\n        cluster.forEach((node, ni) => {\n            let x, y;\n            if (cluster.length === 1) {\n                x = cx; y = ccy;\n            } else {\n                const angle = (2 * Math.PI * ni) / cluster.length - Math.PI / 2;\n                x = cx + radius * Math.cos(angle);\n                y = ccy + radius * Math.sin(angle);\n            }\n            node.animate({ position: { x, y } }, { duration: 800 });\n        });\n    });\n\n    animateEnergyDecay();\n    setTimeout(() => { if (cy) cy.fit(cy.elements(), 40); }, 900);\n}\n\n// ── ORBIT: Concentric rings by connectivity ──────────────────\n// Hub nodes (most connections) in the center, leaf nodes on outer rings.\nfunction layoutOrbit() {\n    const nodes = cy.nodes();\n    const centerX = cy.width() / 2;\n    const centerY = cy.height() / 2;\n    const maxR = Math.min(centerX, centerY) * 0.85;\n\n    // Sort by degree descending (hubs first)\n    const sorted = nodes.toArray().sort((a, b) => b.connectedEdges().length - a.connectedEdges().length);\n    const count = sorted.length;\n\n    // Split into concentric rings\n    const ringCount = Math.max(1, Math.min(5, Math.ceil(Math.sqrt(count))));\n    const rings = [];\n    const perRing = Math.ceil(count / ringCount);\n\n    for (let i = 0; i < count; i++) {\n        const ri = Math.min(Math.floor(i / perRing), ringCount - 1);\n        if (!rings[ri]) rings[ri] = [];\n        rings[ri].push(sorted[i]);\n    }\n\n    rings.forEach((ring, ri) => {\n        const r = (ri === 0 && ring.length <= 2)\n            ? 0  // hub nodes sit at dead center\n            : maxR * ((ri + 0.5) / ringCount);\n\n        ring.forEach((node, ni) => {\n            const angle = (2 * Math.PI * ni) / ring.length - Math.PI / 2;\n            const x = r === 0 ? centerX + (ni * 40 - (ring.length - 1) * 20) : centerX + r * Math.cos(angle);\n            const y = r === 0 ? centerY : centerY + r * Math.sin(angle);\n            node.animate({ position: { x, y } }, { duration: 800 });\n        });\n    });\n\n    animateEnergyDecay();\n    setTimeout(() => { if (cy) cy.fit(cy.elements(), 40); }, 900);\n}\n\nfunction showEmptyState() {\n    document.getElementById('nodeCount').textContent = '0';\n    document.getElementById('edgeCount').textContent = '0';\n    document.getElementById('energy').textContent = '0';\n}\n\n// ═══════════════════════════════════════════════════════════════\n// LIST PANEL\n// ═══════════════════════════════════════════════════════════════\n\nfunction updateListPanel(data) {\n    if (!data || !data.nodes) return;\n    \n    // Group nodes into clusters\n    const clusters = detectClusters(data);\n    \n    const container = document.getElementById('clusterContainer');\n    container.innerHTML = clusters.map(cluster => renderCluster(cluster)).join('');\n}\n\nfunction detectClusters(data) {\n    // Simple clustering based on connections\n    const clusters = [];\n    const visited = new Set();\n    \n    data.nodes.forEach(node => {\n        if (!visited.has(node.id)) {\n            const cluster = {\n                id: node.id,\n                nodes: [],\n                edges: []\n            };\n            \n            // DFS to find connected component\n            const stack = [node.id];\n            while (stack.length > 0) {\n                const currentId = stack.pop();\n                if (visited.has(currentId)) continue;\n                \n                visited.add(currentId);\n                const currentNode = data.nodes.find(n => n.id === currentId);\n                if (currentNode) cluster.nodes.push(currentNode);\n                \n                data.edges.forEach(edge => {\n                    if (edge.source === currentId && !visited.has(edge.target)) {\n                        stack.push(edge.target);\n                        cluster.edges.push(edge);\n                    }\n                    if (edge.target === currentId && !visited.has(edge.source)) {\n                        stack.push(edge.source);\n                        cluster.edges.push(edge);\n                    }\n                });\n            }\n            \n            if (cluster.nodes.length > 0) {\n                clusters.push(cluster);\n            }\n        }\n    });\n    \n    return clusters.sort((a, b) => b.nodes.length - a.nodes.length);\n}\n\nfunction renderCluster(cluster) {\n    const mainNode = cluster.nodes[0];\n    const icon = cluster.nodes.length > 5 ? '🌌' : \n                 cluster.nodes.length > 2 ? '✨' : '⭐';\n    \n    return `\n        <div class=\"cluster-card\" onclick=\"focusCluster('${cluster.id}')\">\n            <div class=\"cluster-title\">\n                ${icon} ${mainNode.label}\n            </div>\n            <div class=\"cluster-stats\">\n                <span class=\"cluster-stat\">\n                    📊 ${cluster.nodes.length} nodes\n                </span>\n                <span class=\"cluster-stat\">\n                    🔗 ${cluster.edges.length} edges\n                </span>\n            </div>\n        </div>\n    `;\n}\n\nfunction focusCluster(clusterId) {\n    // Switch to graph view\n    showGraphView();\n    \n    // Highlight cluster\n    setTimeout(() => {\n        const node = cy.getElementById(clusterId);\n        if (node.length) {\n            cy.fit(node.neighborhood().add(node), 50);\n            node.neighborhood().add(node).addClass('highlighted');\n            \n            setTimeout(() => {\n                cy.elements().removeClass('highlighted');\n            }, 2000);\n        }\n    }, 300);\n}\n\n// ═══════════════════════════════════════════════════════════════\n// VIEW CONTROLS\n// ═══════════════════════════════════════════════════════════════\n\nfunction showGraphView() {\n    document.getElementById('viewGraph').classList.add('active');\n    document.getElementById('viewList').classList.remove('active');\n    document.getElementById('viewTime').classList.remove('active');\n    document.getElementById('listPanel').classList.remove('show');\n}\n\nfunction showListView() {\n    document.getElementById('viewGraph').classList.remove('active');\n    document.getElementById('viewList').classList.add('active');\n    document.getElementById('viewTime').classList.remove('active');\n    document.getElementById('listPanel').classList.add('show');\n    document.getElementById('clusterContainer').style.display = '';\n    document.getElementById('timelineContainer').style.display = 'none';\n}\n\nfunction showTimelineView() {\n    document.getElementById('viewGraph').classList.remove('active');\n    document.getElementById('viewList').classList.remove('active');\n    document.getElementById('viewTime').classList.add('active');\n    document.getElementById('listPanel').classList.add('show');\n    document.getElementById('clusterContainer').style.display = 'none';\n    document.getElementById('timelineContainer').style.display = 'flex';\n    drawTimeline();\n}\n\n// ═══════════════════════════════════════════════════════════════\n// TIMELINE — Interactive, zoomable, with folder grouping\n// ═══════════════════════════════════════════════════════════════\n\nlet tlZoomLevel = 1;\nconst TL_MIN_ZOOM = 0.5;\nconst TL_MAX_ZOOM = 6;\nlet tlCollapsed = {};  // folder collapse state\n\nfunction tlZoom(dir) {\n    tlZoomLevel = Math.max(TL_MIN_ZOOM, Math.min(TL_MAX_ZOOM, tlZoomLevel + dir * 0.4));\n    document.getElementById('tlZoomLabel').textContent = Math.round(tlZoomLevel * 100) + '%';\n    drawTimeline();\n}\n\nfunction tlFitAll() {\n    tlZoomLevel = 1;\n    document.getElementById('tlZoomLabel').textContent = '100%';\n    drawTimeline();\n}\n\nfunction drawTimeline() {\n    const body = document.getElementById('tlBody');\n    const scroll = document.getElementById('tlScroll');\n    const axis = document.getElementById('tlAxis');\n    const title = document.getElementById('tlTitle');\n    if (!body || !scroll) return;\n\n    if (!lastGraphData || !lastGraphData.nodes || lastGraphData.nodes.length === 0) {\n        scroll.innerHTML = '<div style=\"padding:40px;text-align:center;color:rgba(160,200,255,0.4);font-size:14px;\">No data — open a project with tracked files</div>';\n        axis.innerHTML = '';\n        return;\n    }\n\n    const nodes = lastGraphData.nodes.filter(n => n.last_change > 0);\n    if (nodes.length === 0) {\n        scroll.innerHTML = '<div style=\"padding:40px;text-align:center;color:rgba(160,200,255,0.4);\">No timestamp data available</div>';\n        return;\n    }\n\n    title.textContent = '⏱ File Activity Timeline (' + nodes.length + ' files)';\n\n    // Sort by time\n    nodes.sort((a, b) => a.last_change - b.last_change);\n    const minT = nodes[0].last_change;\n    const maxT = nodes[nodes.length - 1].last_change;\n    const range = maxT - minT || 86400; // min 1 day\n\n    // Group by folder\n    const folders = {};\n    const maxSize = Math.max(1, ...nodes.map(n => n.size || 1));\n\n    nodes.forEach(n => {\n        const fp = (n.full_path || n.label || '').replace(/\\\\/g, '/');\n        const parts = fp.split('/');\n        const fileName = parts.pop() || n.label;\n        const folderPath = parts.length > 0 ? parts.slice(-2).join('/') : '(root)';\n        if (!folders[folderPath]) folders[folderPath] = [];\n        folders[folderPath].push({ ...n, fileName, folderPath });\n    });\n\n    // Sort folders by most recent activity\n    const folderKeys = Object.keys(folders).sort((a, b) => {\n        const maxA = Math.max(...folders[a].map(f => f.last_change));\n        const maxB = Math.max(...folders[b].map(f => f.last_change));\n        return maxB - maxA;\n    });\n\n    // Color palette per folder\n    const folderColors = [\n        'rgba(88, 166, 255, 0.9)',\n        'rgba(120, 220, 140, 0.9)',\n        'rgba(255, 160, 80, 0.9)',\n        'rgba(200, 120, 255, 0.9)',\n        'rgba(255, 100, 130, 0.9)',\n        'rgba(80, 220, 220, 0.9)',\n        'rgba(255, 220, 80, 0.9)',\n        'rgba(160, 180, 255, 0.9)'\n    ];\n\n    // Bar width accounts for zoom\n    const barWidthPx = body.clientWidth - 180;\n\n    // ── Build axis ──\n    const tickCount = Math.max(3, Math.round(5 * tlZoomLevel));\n    let axisHtml = '';\n    for (let i = 0; i <= tickCount; i++) {\n        const t = minT + (range * i / tickCount);\n        const pct = ((t - minT) / range) * 100;\n        const d = new Date(t * 1000);\n        const label = d.toLocaleDateString(undefined, { month: 'short', day: 'numeric' });\n        const hourLabel = tlZoomLevel >= 3 ? ' ' + d.toLocaleTimeString(undefined, { hour: '2-digit', minute: '2-digit' }) : '';\n        axisHtml += '<span class=\"tl-axis-label\" style=\"left:calc(180px + ' + pct + '% * ' + (barWidthPx / body.clientWidth) + ')\">' + label + hourLabel + '</span>';\n    }\n    axis.innerHTML = axisHtml;\n\n    // ── Build rows ──\n    // Store file paths in a JS array (avoids backslash escaping issues in HTML attrs)\n    window._tlPaths = [];\n    var pathIdx = 0;\n\n    let html = '';\n    folderKeys.forEach(function(folderPath, fi) {\n        var files = folders[folderPath];\n        var color = folderColors[fi % folderColors.length];\n        var collapsed = tlCollapsed[folderPath] || false;\n        var fKey = fi; // folder index for toggle\n\n        html += '<div class=\"tl-folder ' + (collapsed ? 'collapsed' : '') + '\" data-fkey=\"' + fi + '\" onclick=\"tlToggleFolder(this, this.dataset.fkey)\">';\n        html += '  <span class=\"tl-folder-icon\">▼</span>';\n        html += '  <span class=\"tl-folder-name\">📂 ' + folderPath + '</span>';\n        html += '  <span class=\"tl-folder-count\">' + files.length + ' files</span>';\n        html += '</div>';\n\n        // Store folder key -> name mapping\n        if (!window._tlFolderMap) window._tlFolderMap = {};\n        window._tlFolderMap[fi] = folderPath;\n\n        if (!collapsed) {\n            // Sort files within folder by last_change\n            files.sort(function(a, b) { return b.last_change - a.last_change; });\n\n            files.forEach(function(file) {\n                var pct = ((file.last_change - minT) / range) * 100 * tlZoomLevel;\n                var dotSize = 6 + ((file.size || 1) / maxSize) * 10;\n                var d = new Date(file.last_change * 1000);\n                var dateStr = d.toLocaleDateString() + ' ' + d.toLocaleTimeString(undefined, { hour: '2-digit', minute: '2-digit' });\n\n                // File extension for coloring\n                var ext = (file.fileName || '').split('.').pop() || '';\n                var extColors = { ts: '#3178c6', js: '#f7df1e', py: '#3572A5', tsx: '#61dafb', css: '#264de4', html: '#e34c26', json: '#292929', md: '#083fa1' };\n                var dotColor = extColors[ext] || color;\n\n                // Store path in array, use index in HTML\n                var idx = pathIdx++;\n                window._tlPaths[idx] = file.full_path || '';\n\n                html += '<div class=\"tl-row\" data-idx=\"' + idx + '\" data-date=\"' + dateStr + '\" data-size=\"' + (file.size || 0) + '\" data-name=\"' + (file.fileName || '').replace(/\"/g, '&quot;') + '\">';\n                html += '  <div class=\"tl-row-name\" title=\"' + (file.fileName || '') + '\" onclick=\"tlRequestHistoryByIdx(' + idx + ')\" style=\"cursor:pointer;\">';\n                html += '    ' + (file.fileName || '').replace(/\\.[^.]+$/, '') + '<span class=\"tl-row-ext\">.' + ext + '</span>';\n                html += '  </div>';\n                html += '  <div class=\"tl-row-bar\">';\n\n                // Gridlines (light vertical lines)\n                for (var i = 0; i <= tickCount; i++) {\n                    var gPct = (i / tickCount) * 100 * tlZoomLevel;\n                    html += '<div class=\"tl-gridline\" style=\"left:' + gPct + '%\"></div>';\n                }\n\n                // The dot\n                html += '    <div class=\"tl-dot\" style=\"left:' + pct + '%;width:' + dotSize + 'px;height:' + dotSize + 'px;background:' + dotColor + ';\" ';\n                html += '         onclick=\"tlRequestHistoryByIdx(' + idx + ')\"';\n                html += '         onmouseenter=\"tlShowTip(event, this.parentElement.parentElement)\" onmouseleave=\"tlHideTip()\"></div>';\n\n                // Trail line from left edge to dot\n                html += '    <div style=\"position:absolute;top:50%;left:0;width:' + pct + '%;height:1px;background:linear-gradient(90deg,transparent,' + dotColor.replace('0.9', '0.15') + ');pointer-events:none;\"></div>';\n\n                html += '  </div>';\n                html += '</div>';\n            });\n        }\n    });\n\n    scroll.innerHTML = html;\n}\n\nfunction tlToggleFolder(el, fkey) {\n    var folder = (window._tlFolderMap && window._tlFolderMap[fkey]) || fkey;\n    tlCollapsed[folder] = !tlCollapsed[folder];\n    drawTimeline();\n}\n\nfunction tlRequestHistoryByIdx(idx) {\n    var fullPath = window._tlPaths && window._tlPaths[idx];\n    if (fullPath) tlRequestHistory(fullPath);\n}\n\nfunction tlShowTip(event, row) {\n    var tip = document.getElementById('tlTooltip');\n    if (!tip || !row) return;\n    var name = row.dataset.name || '';\n    var idx = row.dataset.idx;\n    var fpath = (window._tlPaths && idx !== undefined) ? (window._tlPaths[idx] || '') : '';\n    var date = row.dataset.date || '';\n    var size = row.dataset.size || '0';\n\n    tip.innerHTML = '<div class=\"tl-tooltip-path\">' + fpath + '</div>'\n        + '<span class=\"tl-tooltip-date\">📅 ' + date + '</span>'\n        + '<span class=\"tl-tooltip-size\">📊 ' + size + ' changes</span>';\n    tip.classList.add('show');\n\n    const rect = document.getElementById('timelineContainer').getBoundingClientRect();\n    let x = event.clientX - rect.left + 12;\n    let y = event.clientY - rect.top - 10;\n    if (x + 300 > rect.width) x = x - 320;\n    if (y + 80 > rect.height) y = y - 80;\n    tip.style.left = x + 'px';\n    tip.style.top = y + 'px';\n}\n\nfunction tlHideTip() {\n    const tip = document.getElementById('tlTooltip');\n    if (tip) tip.classList.remove('show');\n}\n\n// Mouse wheel zoom on timeline\ndocument.addEventListener('DOMContentLoaded', function() {\n    setTimeout(function() {\n        var tlBody = document.getElementById('tlBody');\n        if (tlBody) {\n            tlBody.addEventListener('wheel', function(e) {\n                if (e.ctrlKey || e.metaKey) {\n                    e.preventDefault();\n                    tlZoom(e.deltaY < 0 ? 1 : -1);\n                }\n            }, { passive: false });\n        }\n    }, 500);\n});\n\n// ═══════════════════════════════════════════════════════════════\n// EVENT HANDLERS\n// ═══════════════════════════════════════════════════════════════\n\ndocument.addEventListener('DOMContentLoaded', () => {\n    initParticles();\n    initCytoscape();\n    \n    // Theme buttons\n    document.querySelectorAll('.theme-btn').forEach(btn => {\n        btn.addEventListener('click', () => {\n            document.querySelectorAll('.theme-btn').forEach(b => b.classList.remove('active'));\n            btn.classList.add('active');\n            currentTheme = btn.dataset.theme;\n            cy.style(getCytoscapeStyle());\n        });\n    });\n    \n    // Layout buttons\n    document.querySelectorAll('.layout-btn').forEach(btn => {\n        btn.addEventListener('click', () => {\n            document.querySelectorAll('.layout-btn').forEach(b => b.classList.remove('active'));\n            btn.classList.add('active');\n            currentLayout = btn.dataset.layout;\n            runLayout(currentLayout);\n        });\n    });\n    \n    // View buttons\n    document.getElementById('viewGraph').addEventListener('click', showGraphView);\n    document.getElementById('viewList').addEventListener('click', showListView);\n    document.getElementById('viewTime').addEventListener('click', showTimelineView);\n    \n    // Effect buttons\n    document.querySelectorAll('.effect-btn').forEach(btn => {\n        btn.addEventListener('click', () => {\n            const effect = btn.dataset.effect;\n            applyEffect(effect);\n        });\n    });\n    \n    // Search\n    document.getElementById('searchBox').addEventListener('input', (e) => {\n        filterNodes(e.target.value);\n    });\n\n    // Bug 5: MutationObserver for VS Code theme changes\n    new MutationObserver(() => {\n        if (cy) {\n            cy.style(getCytoscapeStyle());\n        }\n        const isDark = document.body.classList.contains('vscode-dark')\n            || document.body.classList.contains('vscode-high-contrast');\n        document.body.style.background = isDark ? '#0a0a0f' : '#f0f0f5';\n    }).observe(document.body, { attributes: true, attributeFilter: ['class'] });\n\n    // Bug 6: Pause particle animations when tab is hidden\n    document.addEventListener('visibilitychange', () => {\n        if (document.hidden) {\n            if (animationFrame) {\n                cancelAnimationFrame(animationFrame);\n                animationFrame = null;\n            }\n        } else {\n            if (!animationFrame) {\n                animateParticles();\n            }\n        }\n    });\n});\n\nfunction applyEffect(effect) {\n    switch(effect) {\n        case 'pulse':\n            cy.nodes().forEach(node => {\n                const origW = node.numericStyle('width');\n                const origH = node.numericStyle('height');\n                node.animate({ style: { width: origW * 1.4, height: origH * 1.4, 'border-width': 4, 'border-color': '#ff00ff' } }, { duration: 400 })\n                    .animate({ style: { width: origW, height: origH, 'border-width': 0 } }, { duration: 400 });\n            });\n            break;\n        case 'wave':\n            const wave = document.createElement('div');\n            wave.className = 'wave-effect';\n            document.querySelector('.canvas-area').appendChild(wave);\n            setTimeout(() => wave.remove(), 2000);\n            break;\n        case 'explode':\n            cy.nodes().forEach(node => {\n                const angle = Math.random() * Math.PI * 2;\n                const distance = Math.random() * 200 + 100;\n                node.animate({\n                    position: {\n                        x: node.position('x') + Math.cos(angle) * distance,\n                        y: node.position('y') + Math.sin(angle) * distance\n                    }\n                }, { duration: 500 });\n            });\n            setTimeout(() => {\n                runLayout(currentLayout);\n                setTimeout(() => { if (cy) cy.fit(cy.elements(), 40); }, 1500);\n            }, 600);\n            break;\n    }\n}\n\nfunction filterNodes(query) {\n    if (!cy) return;\n    \n    if (query) {\n        cy.nodes().forEach(node => {\n            if (node.data('label').toLowerCase().includes(query.toLowerCase())) {\n                node.style('opacity', 1);\n            } else {\n                node.style('opacity', 0.2);\n            }\n        });\n    } else {\n        cy.nodes().style('opacity', 1);\n    }\n}\n\n// Message handler\nwindow.addEventListener('message', (event) => {\n    const msg = event.data;\n    switch (msg.type) {\n        case 'updateGraph':\n            updateGraph(msg.data, msg.theme, msg.animation);\n            break;\n        case 'changeTheme':\n            currentTheme = msg.theme;\n            cy.style(getCytoscapeStyle());\n            break;\n        case 'runSimulation':\n            runLayout(msg.config);\n            break;\n        case 'fileHistory':\n            tlShowFileHistory(msg.path, msg.history, msg.error);\n            break;\n    }\n});\n\n// ═══════════════════════════════════════════════════════════════\n// TIMELINE — File history detail (expand under row)\n// ═══════════════════════════════════════════════════════════════\n\nlet tlExpandedPath = null;\n\nfunction tlRequestHistory(fullPath) {\n    if (!fullPath) return;\n    // If already expanded — collapse\n    if (tlExpandedPath === fullPath) {\n        tlCollapseDetail();\n        return;\n    }\n    tlExpandedPath = fullPath;\n    // Find the idx for this path and highlight the row\n    document.querySelectorAll('.tl-row').forEach(function(r) { r.classList.remove('tl-row-active'); });\n    if (window._tlPaths) {\n        for (var i = 0; i < window._tlPaths.length; i++) {\n            if (window._tlPaths[i] === fullPath) {\n                var row = document.querySelector('.tl-row[data-idx=\"' + i + '\"]');\n                if (row) row.classList.add('tl-row-active');\n                break;\n            }\n        }\n    }\n    // Remove old detail panel\n    var old = document.getElementById('tlDetail');\n    if (old) old.remove();\n    // Loading indicator\n    var activeRow = document.querySelector('.tl-row-active');\n    if (activeRow) {\n        var detail = document.createElement('div');\n        detail.id = 'tlDetail';\n        detail.className = 'tl-detail';\n        detail.innerHTML = '<div class=\"tl-detail-loading\">⏳ Loading history...</div>';\n        activeRow.after(detail);\n    }\n    // Request from extension -> server -> DB\n    vsc.postMessage({ type: 'getFileHistory', path: fullPath });\n    // Timeout: if no response in 8 sec, show error\n    setTimeout(function() {\n        var d = document.getElementById('tlDetail');\n        if (d && d.querySelector('.tl-detail-loading')) {\n            d.innerHTML = '<div class=\"tl-detail-empty\" style=\"color:#fa0;\">Timeout — server may not be running</div>';\n        }\n    }, 8000);\n}\n\nfunction tlCollapseDetail() {\n    tlExpandedPath = null;\n    document.querySelectorAll('.tl-row').forEach(function(r) { r.classList.remove('tl-row-active'); });\n    var old = document.getElementById('tlDetail');\n    if (old) old.remove();\n}\n\nfunction tlShowFileHistory(filePath, history, error) {\n    var detail = document.getElementById('tlDetail');\n    if (!detail) return;\n    if (error) {\n        detail.innerHTML = '<div class=\"tl-detail-empty\" style=\"color:#f66;\">Error: ' + error + '</div>';\n        return;\n    }\n    if (!history || history.length === 0) {\n        detail.innerHTML = '<div class=\"tl-detail-empty\">No versions found in database</div>';\n        return;\n    }\n\n    var html = '<div class=\"tl-detail-header\">';\n    html += '<span class=\"tl-detail-title\">📜 ' + history.length + ' versions</span>';\n    html += '<button class=\"tl-detail-close\" onclick=\"tlCollapseDetail()\">✕</button>';\n    html += '</div>';\n    html += '<div class=\"tl-detail-versions\">';\n\n    history.forEach(function(snap, i) {\n        var d = new Date(snap.created_at * 1000);\n        var dateStr = d.toLocaleDateString() + ' ' + d.toLocaleTimeString(undefined, { hour: '2-digit', minute: '2-digit', second: '2-digit' });\n        var sizeKB = snap.file_size ? (snap.file_size / 1024).toFixed(1) + ' KB' : '';\n        var reason = snap.reason || '';\n        var reasonTag = reason ? '<span class=\"tl-ver-reason\">' + reason + '</span>' : '';\n        var isLatest = i === 0;\n        var escapedPath = filePath.replace(/\\\\/g, '\\\\\\\\').replace(/'/g, \"\\\\'\");\n\n        html += '<div class=\"tl-ver-row' + (isLatest ? ' tl-ver-latest' : '') + '\" onclick=\"tlOpenDiff(\\'' + escapedPath + '\\', ' + snap.version + ')\">';\n        html += '  <span class=\"tl-ver-num\">v' + snap.version + '</span>';\n        html += '  <span class=\"tl-ver-date\">' + dateStr + '</span>';\n        html += '  <span class=\"tl-ver-size\">' + sizeKB + '</span>';\n        html += '  ' + reasonTag;\n        html += '  <span class=\"tl-ver-action\">🔍 diff</span>';\n        html += '</div>';\n    });\n\n    html += '</div>';\n    detail.innerHTML = html;\n}\n\nfunction tlOpenDiff(filePath, version) {\n    vsc.postMessage({ type: 'showDiff', path: filePath, version: version });\n}\n"}}exports.NexusGraphProvider=NexusGraphProvider;